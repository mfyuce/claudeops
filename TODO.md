# claudeops — TODO

> Açık iş kalemleri. Tamamlananlar `DONE.md`'ye taşınır.

## Bekleyen karar (öncelikli)

- [ ] **KRİTİK, AÇIK: bulk handover'da hâlâ sadece 1 item başarılı oluyor, diğerleri hata veriyor (guard_lock fix'i YETMEDİ)** (2026-08-31) — `GUARD_LOCK_ACQUIRE_TIMEOUT=60.0` fix'i (bkz. DONE.md 2026-08-31 (3)) deploy edildi, `cops` her denemede (2 kez, canlı doğrulandı: `--resume` + PID değişimi) güvenilir şekilde başarıyla handover oluyor — AMA kullanıcı 3. kez 4 session seçip toplu handover denedi, **dakikalarca bekledi**, yine sadece 1 tanesi (cops) işlendi, diğerleri hataydı (kullanıcı doğruladı: "uzun süre bekledim, yine de bitmedi/hata verdi" — bu SABIRSIZLIK/erken-kontrol DEĞİL, gerçek/kalıcı bir bug).
  **Ekarte edilenler (kod okunarak doğrulandı, BUNLARI TEKRAR ARAŞTIRMA):**
  - `guard_lock()` implementasyonu (`guard.py:25-46`) beklendiği gibi çalışıyor — basit `fcntl.flock` polling, `timeout` parametresi doğru uygulanıyor.
  - `_wait_stable()` (`web.py:281-298`) beklendiği gibi — max `HANDOVER_PROC_WAIT_SECONDS=25.0`, hang riski yok.
  - `_NEEDSHO_CACHE` (`web.py:746`) sadece `needs_ho` DISPLAY alanı için, `_handover()`'ın kendi mantığını etkilemiyor.
  - Client tarafında (ne main'de ne react-ui'de) açık bir `AbortController`/fetch-timeout yok — kod aramasıyla doğrulandı.
  - Kullanıcı bu son denemede masaüstü/lokal'deydi (mobil/tünel değil) — ama React önizleme dev server'ı (5173) o sırada zaten KAPALIYDI (iki kez arka planda kendiliğinden öldü) — yani muhtemelen `main`'deki (eski, vanilla-JS) canlı panel kullanıldı, react-ui DEĞİL. `bulkAct()`'ın (main, `web.py` ~1671-1688) per-item try/catch'i doğru (bir item'ın hatası döngüyü durdurmuyor) — kod-okuma ile ZATEN doğrulandı.
  - `saseppr20260830`'un bu pencerede PID'i değişmiş görünüyordu AMA `kind:"fresh"` (--new, --resume DEĞİL) — yani bu bulk-handover'dan KAYNAKLANMADI, alakasız bir olay (yanlış ipucuydu, izini sürme).
  **Kanıt:** aynı web servisi log penceresinde (restart sonrası, fix canlıyken) tam 2 `BrokenPipeError` var: biri `do_GET`→`/api/status` (alakasız, rutin poll), biri `do_POST`→genel `_json(result)` (satır numarası hangi action olduğunu söylemiyor) — aralarında sadece 12 saniye var, ne eski (5s) ne yeni (60s) guard_lock süresine uymuyor, bu da guard_lock ACQUIRE-timeout'unun (fix ettiğim şey) tek/asıl darboğaz OLMADIĞINI düşündürüyor — muhtemelen BAŞKA bir gecikme/hata noktası var, henüz bulunamadı.
  **Yeni session'a önerilen ilk adım:** kullanıcı canlı reproduce'a "evet" demedi ("todo, yeni session'da başlarız" dedi) — bu yüzden BİR SONRAKİ session'ın İLK işi (kullanıcıdan tekrar onay alıp) **canlı fleet'te kendi kontrollü bir tekrar-üretim**: 3-4 test session'ı (cops/shell HARİÇ — cops bu konuşmanın kendisi, tekrar tetiklerse context kaybı riski) seçip main'deki panelden gerçek bir bulk handover tetikle, `journalctl --user -u claudeops-web.service -f` ile CANLI izle (retrospektif log kazmak yerine), her item'ın TAM hata metnini/HTTP status'unu doğrudan gözlemle. Alternatif/paralel: `_handover()`'a (web.py ~1048) geçici, ayrıntılı `print()`/log satırları ekleyip (kill sonucu, spawn `kind`, `_wait_stable` sonucu ayrı ayrı) SUNUCU tarafında TAM olarak nerede durduğunu gör — client-side network belirsizliğinden bağımsız, en güvenilir yol muhtemelen bu.

  **2026-08-31 devam (canlı fleet'e dokunmadan, kod-okuma + instrumentasyon):** Yukarıdaki "önerilen ilk adım"ın ikinci (log) yarısı UYGULANDI, birincisi (canlı tetikleme) YAPILMADI (kullanıcı onayı yok, cops kendi kendini tetiklemez). Ek doğrulamalar (kod okunarak, TEKRAR ARAŞTIRMA):
  - Server `ThreadingHTTPServer` (her istek ayrı thread) — ama `bulkAct()` (`web.py` ~1684) kesin SEQUENTIAL (`for` içinde `await fetch`, bir sonraki isim ÖNCEKİ tam bitmeden gönderilmiyor) → aynı bulk-run İÇİNDE item'lar arası guard_lock YARIŞI OLMAMALI (N-eşzamanlı-bekleyen açlık hipotezi ELENDİ, bulkAct kodu buna izin vermiyor).
  - `_Handler`'da `protocol_version` override edilmemiş → varsayılan HTTP/1.0, `close_connection` hep `True` → **keep-alive YOK**, her `fetch()` kendi TCP bağlantısını açıyor. Bağlantı-paylaşımı/karışması teorisi ELENDİ.
  - `py/cops service watchdog` sadece `user@<uid>.service`'i (oomd öldürürse) geri açıyor, `guard_once()`/`guard_lock`'a HİÇ dokunmuyor → gizli bir 3. kilit yarışmacısı hipotezi ELENDİ.
  - `_wait_stable()` (`web.py:281`) kod tekrar doğrulandı: `timeout` ile sınırlı, 1s poll, hang riski yok (öncekiyle aynı sonuç).
  - **Yeni/açık hipotez (TEST EDİLMEDİ, sadece kodda mümkün olduğu görüldü):** `BrokenPipeError` SADECE client bağlantıyı ZATEN kapatmışsa olur (server `_json()` yazarken patlar) — yani mümkün ki item 2/3/4 sunucu tarafında GERÇEKTEN başarıyla bitiyor (kill+respawn tamam) ama HTTP yanıtı client'a ulaşmadan bağlantı düşüyor → UI bunu "hata" gösteriyor olabilir, oysa fleet aslında SAĞLIKLI. Bunu ayırt edecek veri YOKTU (eski log sadece "BrokenPipeError oldu" diyordu, hangi fazda/başarılı mıydı bilinmiyordu).
  - **Yapılan değişiklik (deploy edildi, henüz commit EDİLMEDİ — kullanıcı onayı bekliyor):** `_handover()`'a (`web.py` ~1060-1094) her faz için `diag_log()` çağrısı eklendi (`handover_begin/lock_acquired/killed/spawned/wait_stable/done/failed/lock_timeout`, hepsi `elapsed_s` ile) + `do_POST`'un ortak `self._json(result)` yazım noktasına (`web.py` ~2996) `except OSError` sarmalı eklendi (`response_write_failed` event'i, **`result.get("ok")`'u da loglar** — yani client bağlantıyı kaybetse bile sunucu tarafında işlem başarılı mıydı diag.log'da görünür). `claudeops-web.service` restart edilip DEPLOY edildi (fleet PID'leri restart öncesi/sonrası AYNI kaldığı doğrulandı — `KillMode=process` sayesinde canlı session'lara dokunmadı).
  - **Sıradaki adım artık çok daha ucuz:** kullanıcı bir dahaki NORMAL bulk-ho denemesinde (özel bir tetikleme/onay gerekmez, olağan kullanım yeterli) `journalctl --user -u claudeops-web.service` veya panel Tanı sekmesi (`diag.log`) yeni `handover_*`/`response_write_failed` satırlarını gösterecek — her item'ın tam olarak hangi fazda/kaç saniyede olduğu + client koptuğunda sunucu-taraf sonucu netleşecek. Log boşsa (event hiç yazılmadıysa) o da başlı başına bir bulgu (örn. istek hiç `_handover()`'a ulaşmamış olabilir — routing/auth katmanında bir şey var demektir).

- [ ] **`feature/react-ui` — artık PARALEL CANLI deploy, merge kararı hâlâ açık** (2026-08-31, kullanıcı: "amacımız geleceğimiz react ona devam... ikisini iki ayrı tunnel ile gidelim... merge etme şimdilik") — worktree-only `npm run dev` denemesi aşıldı: react-ui artık main'le AYNI şekilde kalıcı deploy edilmiş durumda — `claudeops-web-react.service` (:8766) + `claudeops-tunnel-react.service`, ikisi de systemd `--user`, reboot'ta otomatik (main'in `claudeops-web.service`/`claudeops-tunnel.service`'iyle BİREBİR aynı desen, port hariç). URL: `~/.claude/claudeops/tunnel_url-react.txt` (main: `tunnel_url.txt`). ntfy bildirimleri aynı topic'i paylaşıyor, mesajda `[main]`/`[react]` etiketiyle ayrışıyor. **main'e HİÇ dokunulmadı** (kod tabanı ayrı kaldı) — sadece paralel çalıştırılıyor, karşılaştırma için. Detay + kalıcı unit dosyalarının repo'da TAKİP EDİLMEDİĞİ notu: [[TOBEDECIDED#14]]. **Yeni:** ikisi de artık `server_started_at` alanıyla redeploy sonrası açık sekmeleri otomatik yeniliyor (kullanıcı isteği). **Sıradaki adım:** kullanıcı bir süre ikisini paralel kullanıp karşılaştıracak, sonra merge/"react asıl olsun"/"ikisi kalsın" kararı verilecek — o karar gelene kadar main'deki terminal-görünümü bug'larını (blink/scroll, TODO.md'nin başka maddeleri) react-ui tarafında da AYRI AYRI doğrulamak gerekebilir (aynı xterm.js mantığı taşındı, otomatik düzelmiş OLMAYABİLİR).

## Geliştirme (terminal/sohbet)

- [x] **Terminal popup'ının "Sohbet" sekmesine "son mesaj" / "tüm session" iki seçenek — react-ui'de ✅ DONE, main'de YOK** (2026-08-31 istendi, 2026-09-01 yapıldı, kullanıcı "todo'ları da react'te devam edelim" dediği için SADECE `feature/react-ui`'de uygulandı) — `CliProvider.full_history()` (yeni, `base.py`+`claude_provider.py`, `last_exchange`'le AYNI destekleniyor/desteklenmiyor sözleşmesi, paylaşılan `_transcript_lines()` helper'ı) + `/api/term/chat?mode=last|full` + `ChatView.tsx`'e küçük bir toggle. **Canlı bir gerçek bug yakalanıp düzeltildi:** ilk commit `providers/{base,claude_provider}.py`'yi YANLIŞLIKLA `main` worktree'sinde düzenleyip react-ui'de commit etmişti (iki dosya o worktree'de identical/değişmemiş olduğu için `git add` sessizce no-op oldu) — deploy sonrası `/api/term/chat?mode=full` ANINDA `AttributeError` ile 500 verdi (`journalctl` ile yakalandı), dosyalar main'den kopyalanıp gerçekten commit edilerek düzeltildi, curl ile 54 mesajlık gerçek geçmiş doğrulandı. Detay: [[react-ui-rewrite-unmerged-branch]]. **main'e PORT EDİLMEDİ** (provider dosyaları parity için main'de de var ama main'in `web.py`'si `mode=full`'u hiç çağırmıyor, main'de UI'si yok) — istenirse ayrı bir iş.
- [x] **Terminal mobilde scroll sorunu — react-ui'de İKİ fix uygulandı, main'de touch-action parity, GÖRSEL DOĞRULANAMADI** (2026-09-01, kullanıcı: "react da scroll olmuyor terminal") — react-ui'nin `TerminalView.tsx`'i main'in ESKİ (blink-fix ÖNCESİ) `pollTerm` mekanizmasının 1:1 portuydu, aynı reset()+write() boşluğu ORADA da vardı → main'deki AYNI tek-write()+embedded-clear fix'i react-ui'ye de uygulandı. Ayrıca ikisine de (main+react-ui) `touch-action:pan-y` eklendi — xterm.js'in kendi touch/selection handling'i native touch-scroll'la çakışıyor olabilir (xterm.js'in bundled CSS'inde `.xterm-viewport`'ta `touch-action` YOK, standart bir mitigation). **Görsel/dokunmatik olarak DOĞRULANAMADI** — browser/screenshot aracı yok, gerçek bir mobil cihazda tekrar dene.
- [ ] **Terminal içi URL banner'ının yeri gözden geçirilsin** (2026-08-31, kullanıcı: "url listesi cmd line'a veya altta scrollu bir panel olabilir") — şu an xterm görünümünün ÜSTÜNDE sabit bir banner (en fazla 3 URL). Öneri: komut satırı girişine yakın bir yerde ya da ayrı, kaydırılabilir bir alt panel. Hem main hem `feature/react-ui`'de (`UrlBanner.tsx`) geçerli.

## Kritik bug'lar (devam)

- [ ] **tmux-backed "stop" gnome-terminal penceresini orphan bırakıyor — KOD FİX'İ YAPILDI, canlı doğrulama bekliyor** (2026-08-28 bulundu, 2026-08-31 fix — kullanıcı: "web ui'den stop deyince claude/agy kapanıyor ama cmd line açık kalıyor") — Kök sebep teşhisi (aşağıda korunuyor) doğrulandı: `spawn.py`'nin gnome-terminal komutu `tmux new-session -A -s NAME ...; exec bash`, session silinince attached client döner, `; exec bash`'e düşüp BOŞ pencerede kalıyor. **Fix (`kill.py`):** `kill_session_and_parent`'ın tmux-backed dalına, `tmux_kill_session(name)`'dan SONRA (session zaten silindiği için artık güvenli) `_close_windows_by_exact_title(name)` eklendi — `wmctrl -l` ile title'ı **BİREBİR** (substring DEĞİL — `co` `cops`'un içinde geçiyor, roster'da ikisi de aktif, PID-ancestry yerine title tercih edilmesinin sebebi de zaten bu: claude'un parent'ı paylaşımlı tmux SERVER'a çıkıyor, outer gnome-terminal bash'iyle PID ilişkisi hiç YOK) eşleşen pencereyi bulup `xdotool windowkill` (WM_DELETE DEĞİL — X-seviyesinde, gnome-terminal'in "işlem çalışıyor, emin misin?" onay diyaloğu riskini bypass eder) ile kapatıyor. `time.sleep(0.5)` settle payı var (client'ın exec bash'e düşmesi için). **Doğrulanamadı:** ekran o an kilitliydi ([[layout-needs-unlocked-screen]] — wmctrl/xdotool kilitli ekranda güvenilmez), canlı bir tmux-backed session'da gerçek Stop testi yapılamadı. **Sıradaki adım:** ekran açıkken herhangi bir tmux-backed session'da Stop dene, pencerenin gerçekten kapandığını gözle doğrula; sorunsuzsa bu maddeyi DONE.md'ye taşı.
  Orijinal teşhis (2026-08-28): DONE.md'deki eski TODO-b fix'i (`kill_session_and_parent`'ı paylaşımlı `kill.py`'ye taşımak) NON-tmux mimari içindi. TBD#11 (tmux-backed spawn) sonrası `kill_session_and_parent` tmux-backed session'lar için PID-ancestry'e hiç dokunmuyor (bilerek — paylaşımlı tmux server'ı silme riski), bunun yerine `tmux_kill_session(name)` çağırıyor — kök sebep TODO-b'yle AYNI SEMPTOM, FARKLI mekanizma (parent-PID değil, tmux-attach-client davranışı).
- [ ] **Terminal görünümünde bazı satırlar siyah kutunun sağ kenarını taşıyor (kozmetik)** (2026-08-28, [[terminal-view-refresh-destroys-xterm]] fix'i sırasında fark edildi) — okunabilirliği bozmuyor ama `computeFitFontSize`/`fitContainerToTerm`'in hesapladığı font-size ile gerçek container genişliği arasında küçük bir uyumsuzluk olabilir. Düzeltilmedi, düşük öncelik.
- [ ] **web panel Terminal: CLI meşgulken (busy) ekran "blink" ediyor — İKİ AYRI ALT-SORUN, biri ZATEN DÜZELMİŞ, biri kısmen iyileştirildi** (2026-08-28 bulundu, 2026-08-31 güncellendi) — orijinal rapor (kullanıcı: "web den bakilan terminal cok blink yapior txt ye komut bile yazilamiyor. Yazilinca blink sonrasi silinoyr") aslında İKİ farklı mekanizmaydı, bugünkü kod okumasında ayrıştırıldı:
  1. **Input kutusu siliniyor/odak kayboluyor → BU MADDE YAZILDIKTAN SONRA (2026-08-29/31 arası, ayrı bir oturumda) ZATEN DÜZELTİLMİŞ**, TODO.md hiç güncellenmemişti. `render()`'da (`web.py:1586-1624`) `openInput`/`openXterm` sakla→innerHTML-sonrası-geri-koy deseni var, odaktaysa `.focus()`+`setSelectionRange` ile imleç konumu da geri yükleniyor. Bu maddeyi TEKRAR araştırma — kod zaten bu sorunu ele alıyor.
  2. **Ekran görsel "blink"i (CLI "Computing…" durumundayken spinner/süre/token-sayacı her ~200ms tick'te değiştiği için `pollTerm`'ün reset+write'ı neredeyse sürekli tetikleniyor):** 2026-08-31 kısmen iyileştirildi — `term.reset()` (senkron, ANINDA temizler) + ayrı `term.write()` (kendisi async) ikilisi, aralarında tarayıcının boş bir frame render edebileceği bir boşluk yaratıyordu; fix, temizleme escape kodlarını (`\x1b[H\x1b[2J\x1b[3J`) ayrı reset() yerine YAZILAN VERİNİN İÇİNE gömüp TEK write() çağrısına indirdi (`web.py` ~2253, `pollTerm`). **Node --check ile render edilmiş PAGE_HTML üzerinden JS sözdizimi doğrulandı** ([[web-embedded-js-escaping-trap]] dersine uyarak ham dosya değil, GERÇEK render edilmiş string) — ama GÖRSEL olarak (gerçek tarayıcıda blink azaldı mı) DOĞRULANAMADI, browser/screenshot aracı yok. **Aşağıdaki mobil-scroll maddesiyle AYNI koda (`pollTerm`'ün reset/rewrite mekanizması) dokunuyor** — etkileşimi belirsiz, kullanıcı geri bildirimi bekleniyor.

- [ ] **web panel Terminal: mobilde scroll/refresh'te "en üste" zıplıyor — KÖK NEDEN ZATEN TEŞHİS EDİLMİŞ, gerçek fix hâlâ AÇIK** (2026-08-31, kullanıcı canlı tünelden: "terminal yine mobilde en üste wcroll oluyor") — bu YENİ bir bug değil, "yine" diyor + kod içinde ZATEN bir teşhis yorumu var (`web.py` ~2377-2382, `pollChat`'in hemen üstünde, aynı gün daha erken bir oturumda yazılmış): **"kök neden xterm.js'in kendi viewport/reset-rewrite mekanizması (pollTerm)"** — o teşhisin SONUCU olarak gerçek fix yerine PARALEL bir görünüm eklenmiş: "Sohbet" sekmesi (düz HTML/`renderChatView`, tarayıcının native scroll'u, xterm viewport state'i YOK) — mobilde scroll önemliyse ŞİMDİLİK önerilecek workaround bu. **Açık:** `pollTerm`'ün asıl mekanizması (yukarıdaki blink maddesindeki `\x1b[H\x1b[2J\x1b[3J`+write fix'i DAHİL, ikisi AYNI kod bloğu) hâlâ düzeltilmedi — mobil tarayıcıda gerçekten test edilip xterm.js'in viewport/scroll state'inin reset+rewrite'tan NASIL etkilendiği netleştirilmeli. **Sıradaki adım:** kullanıcıdan Sohbet sekmesinde de aynı zıplama olup olmadığını öğren — oluyorsa kök neden bu teşhisten FARKLI bir yerde (belki `render()`'ın 4s'lik `openInput.focus()` çağrısı, `web.py:1620` — mobilde `.focus()` odaklı elemanı görünüme kaydırma davranışı tetikleyebilir); olmuyorsa teşhis doğrulanmış olur, gerçek fix pollTerm'ün xterm viewport/scroll-position yönetimine gitmeli.
- [x] **saseppr/agy: başlatma "açıldı" dedi ama pencere yok ✅ DONE** (2026-08-28) — `spawn.py`'ye retry (3 deneme) + tek-tık "pencere aç" butonu + son-15dk'da 2+ tam-fallback olursa panel-geneli uyarı banner'ı eklendi, canlı doğrulandı (`mo20260828`+`saseppr20260828_2` düzeltildi). Detay: DONE.md "spawn güvenilirliği" bölümü. Aşağıdaki analiz özgün teşhis süreci olarak (yanlış ilk tanı dahil) tutuluyor. ~~TEK SEFERLİK gnome-terminal-server flake'i (reboot sonrası TAZE server'da da tekrarladı), sessiz fallback kullanıcıyı gereksiz retry'a itiyor~~ (2026-08-28, kullanıcı: "saseppr hem claude baslattim onu gordum ama bir de ayni folder da agy actim actim dedi ama penceresini gormedim") — ⚠ İLK teşhisim (bu maddenin önceki hâli) YANLIŞTI, kullanıcı düzeltti: makine `08:49:53`'te restart edilmiş; gnome-terminal-server ŞU AN tek ve TAZE bir proc (pid 23582, `08:51`'de başlamış) — CLAUDE.md RFH'deki "pid 4693 hâlâ bozuk" durumu artık GEÇERSİZ (reboot temizledi), o yüzden "restart et" önerisi de geçersiz. Buna RAĞMEN aynı taze gt-server'la: boot-recovery'nin açtığı 5 session (cops/hc/mo/rustrino/trino, hepsi `08:52-53`) + `saseppr20260828` (claude, `08:59:36`) sorunsuz pencere aldı, ama `saseppr20260828_2` (agy, `09:07:50` — kullanıcının "actim" dediği) YİNE `spawn_fallback_used` ile windowless'e düştü (`diag.log` + canlı `wmctrl -l`/`tmux ls` ile doğrulandı: tmux session var ama "attached" değil, `wmctrl`'de pencere yok). Yani kök sebep kalıcı/eski bir bozukluk değil — taze serverda bile ARA SIRA olan tek seferlik bir flake (`spawn.py` yorumundaki "ARADA SIRADA olur" tam bu). Güncel durum (canlı doğrulandı, `~09:45`): `saseppr20260828_2` düzgün KAYITLI (`./cops list`: model gemini-3.1-pro-high, "dup yok") — ne orphan ne kayıtsız, sadece penceresiz/tmux-only çalışıyor (önceki "kayıtsız `agy-38204`" görünümü geçiciydi, RC-kaydı tamamlanınca düzeldi — bilinen [[handover-edge-cases]] "yavaş kayıt" ailesiyle aynı sınıf). Daha önce görülen `saseppr20260828_1` artık listede yok (ne olduğu netleşmedi, muhtemelen kullanıcı kapattı) — temizlik gerektiren bir dup KALMADI. Asıl kalan UX açığı: fallback SESSİZCE oluyor, kullanıcı anında fark etmiyor → tekrar deneyip gereksiz ikinci session açabiliyor (muhtemelen `_1`→`_2` geçişi tam bu yüzden oldu). Fix önerisi: spawn API yanıtına anında "⚠ pencere açılamadı, tmux-only modda başladı, Terminal butonundan eriş" uyarısı ekle (Tanı sekmesine gitmeye gerek kalmadan) — [[spawn-zombie-child-degrades-web-server]]/[[diag-tab-feature]] ailesiyle ilişkili ama "restart gt-server" bu spesifik flake'i ÇÖZMEZ (kanıtlandı), sadece anlık kullanıcı bildirimi çözer.
- [ ] **mo/RC bridge geç kayıt → remote'da "açık değil" sanılıyor** (TODO-t, 2026-06-21, kullanıcı) — mo53 açık+tek proc ama RC bridge dakikalarca `-` kaldı → kullanıcı remote'da (claude.ai) göremedi, "mo açık değil / multiple cli mi?" dedi. Kök: RC bridge kaydı yavaş (TODO-l/k ile aynı aile). Fix: rc/handover spawn sonrası bridge kaydını poll'la doğrula (TODO-l), VEYA list'te "RC kaydı bekleniyor" durumunu göster (`-` yanıltıcı). Multiple-cli yok (doğrulandı, dup kontrolü temiz).
- [ ] **claude.ai'de ESKİ bridge birikimi → remote'da "duplike/açık değil" yanılgısı** (TODO-u, 2026-06-21, kullanıcı) — her handover claude.ai sunucusunda yeni RC bridge yaratıyor, eskiler silinmiyor. 5 nesil churn (*49→53) sonrası remote'da anomaly50/51/52/53 hepsi birikip "duplike CLI" gibi görünüyor; güncel olan kalabalıkta kayboluyor. LOKAL temiz (27/27 tek proc + RC). Kök: server-side bridge cache temizlenmiyor + aşırı churn. Fix: (a) churn'ü azalt (seyrek handover); (b) handover'da eski-suffix bridge'leri server'dan kapat/teardown (API varsa); (c) kullanıcı app'ten stale sohbetleri elle dismiss. İlgili [[handover-edge-cases]] server-side bridge cache.
- [ ] **handover Faz 1'i gruplara böl / throttle — mass-Faz1 → sunucu rate-limit** (TODO-v, 2026-06-21, KRİTİK) — 24 session'a aynı anda `handover --force` → **sunucu rate-limit** ("Server is temporarily limiting requests", usage-limit DEĞİL) → 3 session (qve/emrgence/anomaly54) **blank-TUI hang** (jsonl son=`user`, terminal boş, jsonl'e error YAZILMAZ → tarama göremez, sadece TUI/remote banner). Fix: handover'da **batch-size + aralara bekleme** (ör. 5'erli grup, gruplar arası ~30-60s) VEYA rate-limit görülünce backoff+retry; + **stuck-detect** (jsonl son=user + düşük CPU + N s geçti → otomatik tek-tek retry, guard.lock KESİNTİSİZ tutarak). Yeni claudeops'ta (TBD#8) throttle/retry/stuck-recovery built-in olmalı. ⚠ **AÇIK (2026-06-21): emrgence53 + anomaly54 hâlâ stuck/elle-recovery bekliyor** (kullanıcı manuel yapıyor). [[mass-faz1-ratelimit-stuck]]

- [ ] **cmd_handover başında `uptime -s` reboot-check** (TODO-p, 2026-06-19, kullanıcı isteği) — handover (özellikle Faz 2 `rc --new`) başlamadan önce boot zamanını kontrol et: makine ≤~30dk önce reboot olduysa **DUR + uyar** ("cron/boot recovery zaten en son halden toparlıyor; elle ho taze reboot üstüne = post-boot artefakt + race + context kaybı"). `--force` ile baypas. Şu an bu kontrol elle yapılıyor (operatör `uptime -s`'e bakıyor) — koda taşı. [[reboot-no-handover]] dersi (2026-06-18 mo50 olayı). İdeal: needs-ho/handover çıktısının başında "boot Xs önce" satırı + danger pencerede kırmızı uyarı.
- [ ] **Faz 1 reopen `--model`/`--permission-mode=auto`/`--effort=max` GEÇMİYOR** (TODO-q, 2026-06-19, kullanıcı) — `cmd_handover` wrap-up reopen'ı `claude --resume <sid> -n <ad> --remote-control <ad> '<prompt>'` ile açıyor; **model/permission/effort flag'leri YOK** → wrap-up session'ları varsayılan model + plan-mode'da açılıyor (auto/max değil). Doğrulandı 2026-06-19 hc51 wrap-up proc'unda flag yoktu. Fix: reopen'a models.tsv'den modeli + `--permission-mode=auto --effort=max` ekle (Faz 2 rc gibi).
- [ ] **Handover öncesi guard cron'u DISABLE et + bekle** (TODO-r, 2026-06-19, kullanıcı) — guard.lock yarışından (TODO-j) daha kesin: `cmd_handover` başında guard cron'u geçici kapat (crontab comment / disable-flag dosyası), **birkaç saniye bekleyip guard'ın devreye GİRMEDİĞİNİ doğrula**, SONRA reopen'lara başla; handover bitince guard'ı geri aç. Bugün hc51 dup tam bu yüzden (guard Faz 1 ortasında respawn etti). TODO-j+TODO-r birlikte çözülmeli.

- [ ] **BUG: `rc <a,b,c>` virgül-separated isim listesi parse edilmiyor** — 2026-05-19'da 15→16, 2026-05-23'te 20→21 transition'larında doğrulandı. `cmd_rc`/`resolve_targets` SPACE-separated bekliyor. CLAUDE.md eski örnekler virgüllü idi → güncellendi. Fix: cmd_rc başında target ve "$@" içindeki virgüllü string'leri split et (IFS=','). Aynı bug `cmd_kill`, `cmd_compact`, `cmd_send` için de var.
- [ ] **`claudeops layout` orphan terminal kaldırmıyor** — 2026-05-19/05-23/05-25 transition'larında doğrulandı (ws1'de "fatihyuce@483-LNX: ~" bir quad slot işgal ediyor). Fix: layout iterasyonunda window-name'in geçerli session.json'da olup olmadığını kontrol et + yoksa skip (oth_wins'e ekleme).
- [ ] **`cancel` Esc güvenilmez (VTE reject)** — 2026-05-25 kulturiot23 "waiting" modal'da Esc inmedi. Garantili iptal = respawn. Fix: cancel Esc dene → 2s sonra hâlâ takılıysa otomatik `rc <name> --kill-first` öner/yap (flag ile).
- [x] **`handover --exclude=<name>` filtrelemiyor ✅ DONE** (2026-08-25) — bash exclude eşleşmesi artık base+suffix varyantlarını yakalıyor (`^(isim)[0-9]*(_[0-9]+)*$`); default exclude listesi de kaldırıldı (opsiyonel flag olarak duruyor). Tarihsel bağlam (2026-06-08) (*36→*37) `--exclude=anomaly` (base-name) verildi ama anomaly36 yine wrap-up edildi (pre-check sınıflandırmasında HO listesinde kaldı). Muhtemelen exclude suffix'li tam isim (`anomaly36`) bekliyor ya da parse hiç çalışmıyor. Fix: `cmd_handover` exclude eşleşmesini base-name + suffix'li ikisini de kabul edecek şekilde düzelt (handover hedef döngüsünde isim normalize).
- [ ] **KRİTİK (j kaldı): rc guard.lock'u KAYIT bitene kadar tutsun** (TODO-j, 2026-06-09 *37→*38) — ~~(h) `exec 9>guard.lock; flock 9` `cmd_rc` başında YAPILDI (2026-06-17, `93f99df`)~~. (j) hâlâ açık: spawn bitince lock serbest bırakılıyor ama RC bridge kaydı dakikalarca sürebiliyor → guard yarışıp dup açabiliyor. Fix: spawn'lardan sonra **beklenen N session `list`'te kayıtlı olana kadar poll** (timeout ~5dk), SONRA `flock -u`. Memory [[handover-edge-cases]] edge-9. **⚠ 2026-06-19: `cmd_handover` guard.lock'u HİÇ tutmuyor** (sadece `cmd_rc` tutuyor) → Faz 1 reopen sırasında guard yarışıp **hc51 dup açtı** (25s sonra --model'li respawn, aynı sid c111d2ba; elle temizlendi). Fix bunu da kapsamalı: `cmd_handover` reopen döngüsünü de `exec 9>guard.lock; flock 9` ile sar.
- [x] **handover Faz1 başarı = PROC-varlığı (bridge değil) ✅ DONE** (TODO-k, 2026-06-21) — false-negative "bridge bulunamadı → failed=1" düzeltildi (hc53 testi: session AÇIK ama bridge geç kaydoldu → "failed" diyordu). Başarı kriteri artık **proc-presence** (`ps|grep -P '^claude' | grep --remote-control NAME(\s|$)`, DONE.md dersi: bridge-field gecikmeli); bridge sadece URL için bilgilendirici ("bridge-pending" if yok). Substring tuzağı yok (space/EOL anchor; hc53≠hcr53 doğrulandı). Sadece proc gerçekten yoksa "failed-noproc". (`cmd_handover` ~1489)
- [ ] **rc/handover: spawn sonrası RC-kayıt yavaş → kaydı doğrula sonra dön** (TODO-l, 2026-06-09) — rc'nin `=== bridge check ===` adımı *38'de BOŞ döndü (kayıt henüz olmamıştı); yine de "bitti" deyip döndü. Fix: bridge-check'i kayıt tamamlanana kadar (poll + timeout) beklet; eksik kalanları WARN'la. (Yukarıdaki guard.lock-tutma fix'iyle birlikte.)
- [x] **cron/boot recovery artefakt-skip ✅ DONE** (`4f0543b` → DONE.md; opsiyonel `uptime` boot-anchor yapılmadı) (TODO-o) — `_latest_sid_for_cwd` (guard satır ~2013 + boot ~1864 kullanıyor) **en yeni mtime**'ı seçiyor. Reboot sonrası bu YANLIŞ: boot SONRASI oluşan boş/thin spawn-artefaktı (ör. ilk-user="session", 0 gerçek user mesajı) en yeni mtime'a sahip olup gerçek 808-satırlık pre-boot konuşmanın önüne geçiyor → session boş context'le açılıyor (2026-06-18 mo50: 1e6e54b7=808 satır orphan, 2d85e4b1=35 satır fresh açıldı). **Fix:** aday jsonl "artefakt" ise atla — artefakt = **hiç gerçek user turn'ü yok** (system-reminder + wrap-up promptu + tek-kelime "session" hariç substantive free-text user mesajı 0). Aday kalmazsa mevcut davranışa düş. Alternatif/ek: `uptime -s` boot-anchor — reboot-recovery'de sadece `mtime < boot` adayları al (steady-state guard'da post-boot işi resume etmeli, o yüzden boot-anchor SADECE boot-yakını pencerede). Kullanıcı kuralı (2026-06-18): "restart oldu → cron en son nerede ise ordan başlasın, ho olsun olmasın; restart-anındaki konuşma neyse o." ⚠ Steady-state'te (session mid-work crash) en son = post-boot olabilir → artefakt-skip boot-anchor'dan daha güvenli (ikisinde de çalışır). İlgili [[reboot-no-handover]].
- [x] **~~handover co + ulaksec'i base-name ile hard-exclude etsin~~ SÜPERSEDED** (TODO-n; 2026-08-25 kapatıldı) — isim-bazlı hariç-tutma TAMAMEN kaldırıldı (kullanıcı kararı: hedef seçimi web panel checkbox'larıyla); yerine process-bazlı self-koruma (`ancestor_pids()`, py handover+rc; bash `filter_not_self`). ulaksec artık kodla değil dikkatle korunuyor. Tarihsel bağlam (2026-06-18) — co + ulaksec models.tsv'de aktif (guard crash-recovery'de ayakta tutsun — İSTENEN davranış, kullanıcı 2026-06-18). AMA handover ASLA dokunmamalı. Eskiden co/ulaksec fleet'ten farklı suffix'te (co43 vs fleet49) olduğu için doğal atlanıyordu; guard die olunca onları **fleet suffix'ine bumplıyor** (co43→co50, ulaksec43→ulaksec50, çünkü guard `base+suffix` çalışmıyorsa resume eder). Artık `handover --from-suffix=50` co50+ulaksec50'yi eşler: co50 `filter_not_self` ile atlanır (self güvende) ama **ulaksec50 self değil → ho listesine girer = "dokunma" ihlali**. Fix: `cmd_handover`'da sabit `HO_EXCLUDE="co ulaksec"` base-name listesi, `filter_not_self`'ten sonra base-name (suffix-stripli) eşleşeni rows'tan düş. (TODO-d `--exclude` base-name parse'ıyla birleşebilir.) Kök sebep ayrıca: guard cwd-tespiti *50 cutover yoğunluğunda co43/ulaksec43'ü bir an ıskaladı → bumpladı.
- [ ] **handover `sid=-` → cwd-based jsonl fallback** (TODO-m, 2026-06-17) — `find_jsonl "$sid"` `sid=-` olunca boş dönüyor → `jsonl=no` → `needs_ho` SKIP → iş yapan yeni session'lar (sase49, marwan49 vb.) Faz 1'den atlıyor. Fix: `sid` boş/`-` ise cwd'den encode edilmiş proje path'ini türet → `~/.claude/projects/<encoded>/` altındaki en yeni `.jsonl`'ı kullan; encoding `add-session-to-fleet` kuralına uy (`/`→`-`, `_`→`-`). Etkilenen: `find_jsonl`, `needs_ho` sinyal tablosu, `handover_done`, `cmd_handover`'daki `--resume $sid` satırı.
- [ ] **sase Faz1'de atlanıyor — needs_ho false-negative? (kontrol)** (2026-06-23, kullanıcı) — sase55 bugünkü Faz1'de **skip** edildi (needs_ho=False: RFH+repo temiz+yeni commit yok), ama kullanıcı "sase'yi kaçırdık, atlandı" dedi → sase'de kaydedilmemiş/commit'lenmemiş iş olabilir; needs_ho yanlış "temiz" demiş olabilir. Kontrol: sase reposunun gerçek durumu (uncommitted? RFH güncel mi? son user turn'ünden sonra iş var mı?). TODO-m'den **farklı mekanizma** (orada `sid=-`→jsonl=no; burada jsonl VAR, sid=519ec3d5 çözüldü) ama **aynı sonuç**: sase handover'dan düşüyor. Tekrar ediyorsa needs_ho sinyal eşiğini sase için doğrula.
- [ ] **layout title-bazlı eşleşme bayat başlığa KIRILGAN → PID ile eşle** (2026-06-23, kullanıcı "sase atlandı") — cross-suffix jsonl resume eden session'ın (rustrino55/sase55) TUI terminal başlığı eski suffix'te (*53) takılı kalıyor → `layout` pencere→session eşleşmesini **başlıktan** yaptığı için `known_names`(*55) ile eşleşmez → "claude değil" sayıp atlar (rustrino bir PIN'di, ws0'a gitmedi). Fix: `_list_windows`+`build_layout_plan` eşleşmeyi **PID** ile yapsın (`wmctrl -l -p` → window PID; ama gnome-terminal'de tüm pencereler server-PID paylaşır → alternatif: VTE pencere↔proc eşleşmesi başka yoldan, ör. `_NET_WM_PID` server olduğu için işe yaramaz; title fallback kalır). ⚠ **AÇIK:** rustrino55(0x0400682c)+sase55(0x0400a4c8) hâlâ elle yerleştirme bekliyor (`xdotool windowmove <wid>` ile; main layout 25/27 uygulandı).
- [ ] **resume cross-suffix jsonl → TUI başlığı BAYAT kalıyor** (2026-06-23) — recovery/Faz2 eski-suffix (*53) konuşmayı `--resume` ile *55 olarak açınca claude TUI terminal başlığını eski isimde (*53) bırakıyor (`-n *55` override'a rağmen, başlıkta aktif ✳ spinner var = canlı proc). Sonuç: title-bazlı TÜM araçlar (layout; muhtemelen remote görünüm) bu session'ları kaçırıyor. Araştır: claude başlığı `-n`'den mi jsonl session-meta'dan mı çekiyor → düzeltme spawn-side (resume'da title re-emit) mı tool-side (PID-eşleşme) mı. [[guard-cron-relative-path]] değil, ayrı.

- [ ] **config-corruption: eşzamanlı `~/.claude.json` yazması config'i BOZUYOR** (2026-06-13, [[config-corruption-resume-hang]]) — toplu handover/respawn'da onlarca claude config'e aynı anda yazınca **truncated JSON** → `claude --resume` startup'ta **BLANK-TUI hang** (fresh `--new` çalışır → yanıltıcı; teşhis `claude --resume <sid> -p x --debug`). Manuel fix: `~/.claude/backups/`'tan en yeni VALID backup'ı restore. **KOD FİX:** handover/rc respawn'larını **serialize et** (tek-tek, her birinden sonra bridge-kaydını bekle) + her adımda `python3 json.load(~/.claude.json)` config-check → bozulursa DUR + backups'tan auto-restore. (TODO-h+j lock-tutma ile birleşik.) ⚠ Done-tespiti: jsonl-stale **≥150s** (60s erken — session iş ortasında 60s+ duraklıyor → çakışma); `status` lag'liyor; en güvenilir done = kullanıcı-gözü. Ek tuzaklar: `import -window` (Mutter) stale-buffer YANILTICI; `ps|grep|kill`/`pkill -f` komut-metni session-adı içerince **kendi shell'ini ($$) öldürür** → `$$` hariç tut / PID ile.

- [ ] **`needs_ho` sıfır-mesajlı session'ı skip etmeli** (2026-06-25) — `--new` ile açılan boş/idle session'ların hiç substantive user turn'ü yok ama RFH da yok → `needs_ho=True` → `handover --dry-run` hepsini "WOULD handover" gösteriyor (yanlış pozitif). Fix: jsonl'de gerçek user turn sayısını say; 0 (veya sadece system-reminder/spawn artefaktı) ise `needs_ho=False` + skip. Skip kriteri `needs_ho`'nun bir parçası olmalı.
- [ ] **rc `--kill-first` sonrası biraz daha bekleme** (2026-06-25, kullanıcı) — kill → spawn arasına ~1-2s ek grace ekle (şu an sadece SIGTERM+8s settle var); lazy-checkpoint flush'un tam bitmesi için [[claude-2183-conversation-truncation]]. Spawn'dan önce kısa `time.sleep(1.5)` veya proc-gone doğrulaması.

## Geliştirme

- [ ] **Provider mimarisine yeni CLI backend'ler (chatgpt/codex, deepseek, gemini, vb.)** (2026-08-30, kullanıcı: "chatgpt cli, deepseek cli etc başka neler var eklenmesi gereken? sence") — mimari zaten hazır (`CliProvider` + registry, bkz. `shell_provider.py`), yeni bir backend eklemek bir provider dosyası yazmaktan ibaret. ⚠ Bu liste 2026-01 model bilgisiyle + bu makinede canlı kontrolle yazıldı, 8 aylık boşluk var — agy'de olduğu gibi CLI yüzeyleri hızlı değişiyor, EKLEMEDEN ÖNCE mutlaka canlı `--help` doğrulaması şart (TOBEDECIDED#10'daki agy dersi: "CLI'lar sabit durmuyor").
  1. **`gemini` (Google Gemini CLI) — EN YÜKSEK ÖNCELİK, bu makinede ZATEN KURULU** (`~/.nvm/.../bin/gemini`, v0.49.0) — `agy`'den (Antigravity) FARKLI, ayrı bir ikili; canlı `--help`'te doğrulandı: `-m/--model`, `--approval-mode default|auto_edit|yolo|plan` (permission_mode'a temiz eşlenir), `-r/--resume [latest|N]`, `--session-id` (isimlendirme için `--remote-control`/`COPS_NAME`'e üçüncü bir alternatif olabilir — env yerine doğrudan cmdline argümanı, agy'den daha kolay), `-p/--prompt` (headless). Sıfır kurulum maliyeti + zaten en somut/hazır aday.
  2. **OpenAI Codex CLI** (`codex` binary) — "chatgpt cli" muhtemelen bunu kastediyor (OpenAI'ın resmi terminal coding agent'ı); bu makinede kurulu değil, gerçek flag'leri doğrulanmadı.
  3. **DeepSeek:** resmi/ayrı bir "DeepSeek CLI" agent ürünü olduğundan emin değilim (varsa adı/olgunluğu doğrulanmalı). Daha olası pratik yol: **Aider** (model-agnostic, açık kaynak, DeepSeek API'sini YERLEŞİK destekliyor) — DeepSeek'e özel bir provider yazmak yerine Aider'ı tek bir provider olarak eklemek muhtemelen daha az iş + bonus olarak başka modelleri de (OpenAI, yerel Ollama, ...) aynı provider'dan kapsar.
  4. Daha düşük güvenle/araştırılmadı: Cursor'ın CLI agent modu, Amazon Q Developer CLI, GitHub Copilot CLI (`gh copilot` — muhtemelen tek-seferlik öneri modunda, claude/agy/gemini gibi kalıcı-interaktif-session değil, bu mimariye uyup uymadığı şüpheli).
- **Öneri:** `gemini`'den başla (kurulu + doğrulanmış + en az sürtünme), ikinci sıra Codex ya da Aider. Her yeni provider agy entegrasyonundaki gibi canlı `--help` + gerçek bir spawn/discovery testiyle doğrulanmalı, tasarım notuna güvenilmemeli.

- [ ] **roster'daki tarih-isimli çöp kayıtları temizle** (2026-08-25) — `rustrino20260825`/`rustrino20260825_1`, `line20260825`, `trino20260824` gibi test/ad-hoc satırlar roster+models.tsv'de birikti; base regex `_N` indirgemesi sonrası bunlar "Kayıtlı" sekmesinde durmuş-gibi görünüyor. Hangi isimlerin kalıcı olacağı kullanıcı kararı — sorup sil/birleştir.
- [ ] **web panel: `config_msg` dil-duyarlı değil** (2026-08-26) — özet satırındaki "config: ~/.claude.json geçerli" metni panel EN'deyken de Türkçe (validate_config `lang` almıyor). Küçük kozmetik.
- [ ] **web panel: kayıtlı proje için "yeniden adlandır" (rename)** (2026-08-26, kullanıcı isteği) — sase→saseppr rename'i elle TSV düzenlemeyle yapıldı; UI'de karşılığı yok. Tasarım: durmuş satırda basit rename (roster+models satır adı değişir); çalışan satırda adopt-benzeri akış (kill → yeni isimle `--resume` respawn → TSV güncelle). `_register_project`'teki isim-çakışma kontrolleri (roster + çalışan-base) aynen uygulanmalı.
- [ ] **web panel: ulaksec satırına "dokunma" uyarı rozeti (opsiyonel)** (2026-08-25) — isim-bazlı kod koruması kalktı; toplu seçimde yanlışlıkla işaretlenmesin diye satırda küçük bir uyarı işareti/onay iyi olur.
- [ ] **web panel: satır durumu sadece "proc canlı mı" değil, CLI gerçekten iş yapıyor mu (busy/idle) göstermeli** (2026-08-28, kullanıcı: "eger cli de is varsa calisior gorunsun yoksa calsmiyor gibi") — `runningRow`'daki durum hücresi (`py/claudeops/commands/web.py:1634`) şu an sadece `<span class="dot on">` + pid basıyor; session idle prompt'ta beklerken de aktif "✻ Computing…/esc to interrupt" durumundayken de aynı görünüyor. Yanında zaten bir `cpu%` kolonu var (`s.cpu`) ama bu güvenilir bir busy sinyali değil (API yanıtı beklenirken network-bound, CPU düşük kalabilir). Daha güvenilir sinyal: `tmux_capture` çıktısının son satırlarında CLI TUI'sinin bastığı "Computing…"/"esc to interrupt" deseni (ekran görüntülerinde canlı görüldü) — bunu arayan basit bir regex ile busy/idle ayrımı çıkarılabilir. Fix: durum hücresine 3. bir görsel durum ekle (busy/idle-running/stopped).
- [ ] **web panel Terminal: modal açılışta oto-resize/font-fit "kayma" yaratıyor — sabit oran ya da oto-resize'ı kapatma seçeneği** (2026-08-28, kullanıcı: "cli web ui den acilinca resize etmeme izin olsun. belki kaymayi engelleyecek ratio buluruz"; 3 ekran görüntüsü `~/Pictures/Screenshots/Screenshot from 2026-08-28 08-40-4{9,53,58}.png`) — görüntülerde terminal içeriği satır satır kaymış/parçalanmış, aralarda büyük boşluklar oluşmuş görünüyor (okunaksız). `pollTerm` gerçek pane boyutu değiştiğinde (`d.cols/d.rows`; bkz. `tmux_pane_size` docstring'i, `py/claudeops/tmux_backend.py:124-129`: attach eden gnome-terminal istemcisi pane'i tmux'un `-x/-y` isteğinden BAĞIMSIZ kendi boyutuna zorluyor) `term.resize()` + `computeFitFontSize`/`fitContainerToTerm` (`web.py:1991-2025`) ile font-size'ı yeniden hesaplıyor; ama `tmux capture-pane` metni ZATEN kaynak pane'in o anki genişliğine göre hard-wrap edilmiş geliyor (`tmux_capture`, `tmux_backend.py:82`). xterm.js'in cols'u resize tetiklenene kadar bir tick GERİDE kalırsa (varsayılan 160'tan gerçek genişliğe geçiş anı gibi), eski genişlikte hard-wrap edilmiş metin yeni/yanlış cols'a yazılır → tam bu "shredded" görünüm. Kullanıcı isteği: oto-resize'ı kapatıp sabit bir cols/rows-to-px oranında tutma seçeneği (drift'i kaynağında önler). Fix adayı: (a) resize algılanan tick'te önce resize'ı uygula, capture'ı BİR SONRAKİ tick'e kadar yazma (yeni genişlikte capture gelsin); (b) kullanıcıya "oto-fit kapalı, sabit font-size" seçeneği/toggle ekle.
- [ ] **web panel Terminal: içteki scrollbar'ı fare ile sürükleyip yukarı/aşağı çekemiyorum** (2026-08-28, kullanıcı: "icteki scrolla basip asagiya yukariya cekrmiyorum"; kullanıcı: acil değil) — muhtemelen xterm.js'in kendi scrollbar'ı/`.xterm-viewport` ile ilgili bir CSS/etkileşim sorunu; araştırılmadı, düşük öncelik.
- [ ] **web panel "Kayıtlı" (Registered) sekmesi: gruplama/tree yok, aynı gruptan çalışan var mı görünmüyor** (2026-08-28, kullanıcı: "Registered da bu grupda acik olan var mi gosterelim. Ayrica registered tree seklinde olsun. Hangi grupda neler var gorelim.") — `registeredTable`/`registeredRow` (`py/claudeops/commands/web.py:1648-1676`) düz liste; roster/session modelinde şu an kalıcı bir "group" alanı YOK (`--group=` sadece `layout`/`handover`'a tek-seferlik geçilen bir CLI argümanı — CLAUDE.md/TODO.md'deki `--group=names@ws` — kayıtlı bir alan değil); en yakın doğal gruplama anahtarı zaten gösterilen `cwd` (aynı repo'ya birden isim, ör. bu repoda cops+diag). ⚠ Netleştirilmeli: kullanıcının kastettiği "grup" cwd-bazlı mı yoksa yeni bir etiket/roster kolonu mu olacak — sorup teyit et. Netleşince: (a) `registeredTable`'ı seçilen anahtara göre gruplayıp katlanabilir (tree) render et; (b) her grup başlığında o gruptaki herhangi bir ismin `runningTable` listesinde olup olmadığını cross-check edip rozetle.
- [ ] **autostart cold-boot hâlâ bash `claudeops`** (2026-06-25, **GÜNCELLEME 2026-08-30: fix yönü değişti**) — `~/.config/autostart/claudeops.desktop` reboot'ta `bash -lc 'claudeops guard --boot --lock'` (BASH tool) çalıştırıyor. Eski plan "py/cops guard --boot --lock'a taşı" idi; kullanıcı (2026-08-30) haklı olarak itiraz etti: artık gerek yok — `py/cops service` ile web panel+tunnel zaten reboot'ta kendiliğinden ayakta, fleet'i telefondan panelden tek tek başlatıyor (2026-08-24 "Fleet kontrolü — MANUEL" kararıyla tutarlı; cold-boot'ta otomatik toplu-reopen o kararın istisnasıydı, artık ihtiyaç yok). **Yeni fix: porta gerek yok, `.desktop` dosyasını kaldır.** Bkz. [[TOBEDECIDED#12]]. [[reboot-recovery]] [[guard-cron-relative-path]]
- [ ] **`deep-ho` komutu (yeni, ayrı cmd)** — 2026-06-01 istek. Normal `ho` sadece wrap-up (commit/push + MD güncel mi) sorar. `deep-ho` ek olarak: her CLI/session'ın TÜM jsonl geçmişini okuyup "kaçırdığımız bir şey var mı?" analizini yaptırır (yarım kalan iş, kaydedilmemiş karar, eksik test/doküman, TODO'ya yazılmamış fikir). Tek komut hem `ho` hem `deep-ho` fazını çalıştırabilmeli (`ho --deep` veya `deep-ho` ayrı dispatch). Her session'a daha uzun/derin bir wrap-up prompt'u gider; çıktı per-session özet + co'ya toplanır.
- [ ] **`boot`/`recover` `models.tsv` lookup** — 2026-06-01 model-split'e dönüldü (`~/.claude/claudeops/models.tsv` name→model), AMA `cmd_boot` hâlâ `BOOT_MODEL_DEFAULT` tek opus kullanıyor. Split kalıcıysa boot her session'ı models.tsv'deki modeliyle açmalı (yoksa default). Aynı şekilde handover Faz-2 elle 2-grup'a bölünüyor → ileride `rc --from-models-tsv` ile tek komut respawn (her isme kendi modeli) düşünülebilir.
- [ ] **`--model` verince default `--permission-mode=auto`** — 2026-05-25: artık HEPSİ auto (sonnet de). Yani mapping basitleşti: `--model=opus|sonnet` verilince permission-mode otomatik `auto` olsun (explicit verilirse override). Şu an her çağrıda elle `--permission-mode=auto` yazılıyor.
- [ ] **Python UI (büyük)** — claudeops için GUI: session listesini göster, tıkla → compact/RC/kill/send butonları, layout görsel önizleme. Stack TBD (PySide6 / Textual / Web). Ana motivasyon: CLI yerine UI.
- [ ] **`claudeops history` + `claudeops launch <name|sid>`** — geçmişte açık olan TÜM session'ları registry'le, `launch` ile yeni gnome-terminal'de RC açar.
- [ ] **`--models=name:model,...` per-name config** — manuel name→model map. Şu an model PLAN array'de gömülü, hatalara açık.
- [ ] **Spawn/layout geometry: KİLİTLİ ekran → Mutter pencere-move BOZUK (KANITLANDI 2026-06-23/24)** — ~~hipotez~~ artık kesin: kilitli ekranda `layout` pencereleri sola üst üste yığıyor, `xdotool windowmove` TAMAMEN reddediliyor, `wmctrl -e` 2×-offset veriyor (sahte "200% scale"), root-screenshot blank. 2026-05-17 HDMI-rows da aynı sebep. **SAATLERCE kaybettirdi** — koordinat/scale "fix"i yanlış yola sokar (artefakt). **Fix:** `py/cops layout` (ve spawn-geometry) başına pre-flight lock-check: `loginctl show-session $(loginctl --no-legend|awk '{print $1;exit}') -p LockedHint` → `yes` ise abort + "önce ekranı unlock et" uyar. [[layout-needs-unlocked-screen]]
- [ ] **`layout`: yatay (yan-yana) çoklu-monitor kurulumunda pencereleri YANLIŞ ekrana/köşeye yığıyor** (2026-08-28, kullanıcı: "layout deyince laptop penceresinde degil de ekli ikinci ekranda sol alta ust uste yigiyor ekranlari. bazialrini da laptop a atiyor") — kök sebep muhtemelen `py/claudeops/layout.py:59-70` `_detect_screen_y`: kendi docstring'i de itiraf ediyor, SADECE dikey-istiflenmiş (biri üstte biri altta, Y farklı) dual-monitor'u ayırt edebiliyor; yan-yana (laptop + eklenmiş ikinci ekran, ikisi de Y=0) kurulumda `unique_y` tek değer (`{0}`) olduğundan sinyal veremiyor. `_get_screen` (`layout.py:73-88`) X offset'i HER ZAMAN `0` alıyor ve `w,h`'ı `xdotool getdisplaygeometry`'den okuyor — bu da (Xinerama-ayrılmamış kurulumlarda) TÜM monitörlerin BİRLEŞİK sanal masaüstü boyutunu döndürür, tek bir monitörünkini değil. Sonuç: 2×2 quadrant matematiği (`quad_pos`) laptop+ek-ekranı TEK bir "ekran" sanıp bu birleşik alanı bölüyor → hangi quadrant'ın fiziksel olarak hangi monitöre denk geldiği rastgeleleşiyor, ızgara yoğunluğu tek-monitör varsayımını aştığında pencereler çakışan koordinatlara yığılıyor (kullanıcının gördüğü "üst üste yığılma"). Bash tarafında (`claudeops` script, `cmd_layout` ~880-931) da AYNI SINIF ama farklı mekanizmalı bir sorun var: `xrandr`'ın "primary" bayrağına ya da ilk "connected" satırına güveniyor, o da yanlış/beklenmedik monitörü seçebilir — hangi yoldan (web paneli → Python `layout.py`, ya da CLI `./claudeops layout` → bash) tetiklendiğine göre ikisi de kontrol edilmeli. Fix: gerçek çoklu-monitor enumerasyonu — `xrandr --query`'den HER connected monitörün tam `WxH+X+Y` geometrisini parse edip (sadece Y değil) `ScreenGeometry` listesi olarak döndür; quadrant planlamasını seçilen monitör(ler)in gerçek sınırları içinde yap. [[layout-needs-unlocked-screen]] ile aynı dosya/aile, farklı belirti — ilişkili.
- [ ] **`layout` ayarlarına "hedef ekran" seçeneği (ana/ek/ikisi) + genel ayar-yüzeyi genişletme** (2026-08-28, kullanıcı: "layout derken pencereleri hangi ekrana yigmali 1-asil ekran, 2-ek ekranlar 3-ikisini birden kullan gibi ayarlarimiz olmali + baska ne gibi ayarlar olabilir dusun ve onlari da ayarlar tabina ekle") — yukarıdaki çoklu-monitor bug'ının düzgün çözümü zaten bunu gerektiriyor: `_run_layout`'ta (`py/claudeops/commands/web.py:680`) hâlâ kullanılmayan bir `screen_y` parametresi var ama web panelinin Layout sekmesi (`renderLayoutBox`, `web.py:2245-2263`) bunu HİÇ göstermiyor/göndermiyor (`doLayout()`, `web.py:2266` POST body'sinde yok) — yani backend'de yarım bırakılmış bir kanca zaten mevcut. Önerilen: monitör enumerasyonu netleşince (yukarıdaki bug'a bkz.) Layout sekmesine bir seçici ekle: **ana ekran / ek ekran(lar) / ikisi birden**. Kullanıcının istediği "başka ne olabilir" beyin fırtınası (henüz uygulanmadı, sadece öneri — netleşince seçilip implementasyona geçirilebilir):
  - **Izgara yoğunluğu** (per-desktop pencere sayısı — bash'teki `per` değişkeninin karşılığı; şu an web'de sabit/gizli) — 2×2 dışında 3×2/3×3 vb. seçilebilir olsun.
  - **"BR köşeyi hep boş bırak"** kuralı — zaten ayrı açık bir TODO ("Geliştirme" bölümünde), aynı ayar-yüzeyine eklenebilir.
  - **Başlangıç workspace/desktop no'su** — zaten ayrı açık bir TODO (`layout --group` desktop no'su serbest-other sayısına bağlı), sabit hedefleme seçeneği aynı forma girebilir.
  - **Ekran-kilit ön-kontrolü göstergesi** ([[layout-needs-unlocked-screen]]) — apply'dan önce "ekran kilitli/açık" durumunu panelde canlı gösterip kilitliyken butonu disable etmek (şu an sadece backend'de sessiz `_screen_locked()` reddi var — kullanıcı reddin NEDENİni panelde görüyor mu kontrol edilmeli).
  - **Monitör başına farklı ızgara yoğunluğu** (büyük ek ekranda 3×2, laptop'ta 2×2 gibi) — daha büyük/isteğe bağlı bir adım, ilk sürümde şart değil.
- [ ] **Auto-respond permission prompts** — OCR çalışıyor (tesseract), keystroke landing intermittent. Seçenekler: OCR + RC API inject, ydotool (Wayland), claude TUI `--auto-accept` flag.
- [ ] Wayland desteği için layout fallback (gdbus + Mutter extension veya hint)
- [ ] Terminal emülatör parametrize (gnome-terminal yerine kitty/alacritty config/env)
- [ ] Rate-limit reset zamanını output'tan parse + auto-resume
- [ ] `claudeops batch --dry-run`
- [ ] `claudeops list --json` machine-readable
- [ ] `claudeops send` stdin'den prompt okuma
- [ ] `claudeops layout` için "BR köşede her zaman boş bırak" benzeri kural
- [ ] **handover `--layout` oto-tile `--group` geçirmiyor** — tek-komut `handover --force --layout` yolunda grup'lar uygulanmaz (ayrı Faz 3 komutu --group içeriyor). Fix: `cmd_handover`'a `--group=` passthrough → `cmd_layout`'a ilet. (2026-05-31, --group eklenince fark edildi.)
- [ ] **`layout --group` desktop no'su serbest-other sayısına bağlı** — 16 serbest-other → grup ws4/5; sayı değişirse kayar (gruplar yine birlikte ama ws no farklı). Sabit istenirse `--group=names@ws` hedefleme ekle (others o ws'i atlar). (2026-05-31)
- [ ] UI'ye Handover textini o an hangi dil secili ise o dilde gosterm tabi. ordan copy paste yapariz. Gerektiginde ayri cli acmadan.

## Dokümantasyon

- [ ] README'ye actual workflow örnekleri (güncel session isimleri)
- [ ] CLAUDE.md'ye "ne zaman compact" rehberi
- [ ] Demo gif/video — visible mode reopen sırası

## Test/Quality

- [ ] Unit test: `find_self_claude_pid` (claude değil bash'tan çağrılınca)
- [ ] Smoke test: tek session aç, kill, compact, RC, doğrula
- [ ] Edge case: 0 session açıkken komut davranışı

## Açık sorular

- [ ] gnome-terminal `--title` flag'i claude TUI tarafından override mı?
- [ ] `--remote-control` flag'i `--name` ile çakışıyor mu? (RC name session name'i de set ediyor görünüyor)
