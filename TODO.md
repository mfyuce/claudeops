# claudeops — TODO

> Açık iş kalemleri. Tamamlananlar `DONE.md`'ye taşınır.

## Kritik bug'lar (devam)

- [x] **web panel: redeploy sonrası bazı sekmelerde "sayfa açılırken hata, tüm sayfa gizleniyor" ✅ DONE** (2026-09-02, kullanıcı canlı bildirdi) — Kök sebep: `_serve_static()` HİÇBİR `Cache-Control` header'ı göndermiyordu — `index.html` (Vite'ın content-hash'li `/assets/*` dosyalarına sabit adlarla referans veren küçük bir kabuk) tarayıcının kendi öngörülemeyen heuristik cache'ine kalıyordu. Bir redeploy (`npm run build`, her seferinde YENİ hash'li dosya adları üretir, ESKİ hash'li dosyaları diskten SİLER) sonrası önbellekte ESKİ bir `index.html` kalan bir sekme, artık var olmayan eski hash'li JS/CSS dosyalarını ister → 404 → React hiç mount olmaz → sayfa boş/"gizli" kalır. Bu oturumda ardışık iki restart yapıldığı için tetiklenme ihtimali özellikle yüksekti. Fix: `/assets/*` (hash'li, içerik değişince adı da değişir) `Cache-Control: public, max-age=31536000, immutable` — sonsuza kadar güvenle cache'lenebilir; `index.html` (ve hash'siz her şey) `Cache-Control: no-cache` — HER ZAMAN taze çekilir. Scratch bir web instance'ında (canlı fleet'e dokunmadan) her iki header da doğrulandı. ⚠ Kullanıcının gördüğü TAM belirtiyi (ekran görüntüsü/repro adımı) canlı gözlemleyemedim — bu kod-okumayla ulaşılan EN GÜÇLÜ hipotez+fix, deploy sonrası kullanıcı teyidi bekleniyor.
- [x] **tmux-backed "stop" gnome-terminal penceresini orphan bırakıyor ✅ DONE (sadece react-ui)** (2026-09-01) — Fix: `tmux_backend.py`'ye `find_outer_bash_pids(name)` eklendi — session öldürülmeden ÖNCE (attach client hâlâ `-s NAME`/`-t NAME`'i kendi cmdline'ında taşırken; `remain-on-exit` kapalıyken pane'in komutu çıkar çıkmaz session zaten kendiliğinden kapanıyor ve `exec bash` argv'yi silip tanınmaz hale getiriyor, `tmux_kill_session` çağrısını bile beklemiyor) o session'a bağlı TÜM gnome-terminal pencerelerinin (ilk spawn + `open_window`'un "pencere aç" ekstra pencereleri) outer-bash PID'lerini cmdline-eşleşmesiyle topluyor. `kill_session_and_parent` artık bunları `kill_session`'dan ÖNCE topluyor, pane öldükten ve `tmux_kill_session` çağrıldıktan SONRA (create_time doğrulamasıyla, PID-reuse'a karşı) her birini `p.kill()` ile kapatıyor. PID-ancestry KULLANILMADI (pane'in gerçek parent'ı paylaşımlı tmux SERVER — TBD#11'in "tüm filoyu silme" riski hâlâ geçerli olurdu); pür cmdline-eşleşme X11/Wayland'dan bağımsız (wmctrl/xdotool yok, [[layout-needs-unlocked-screen]] sınıfı sorunlara bağışık). **Canlı doğrulandı:** tek-pencereli VE iki-pencereli (spawn + open_window) throwaway `shell`-provider test session'ları (`killtest0901`/`killtest0901b`, gerçek fleet'e hiç dokunulmadan) — ikisinde de kill sonrası TÜM pencere(ler) kapandı (orphan bash KALMADI), tmux session temiz silindi; gerçek 6 session'lık fleet (`cops20260901_1` vb.) etkilenmedi. `claudeops-web-react.service` restart edilip canlıya alındı — yazıldığında sadece react-ui worktree'sindeydi, main o zaman aynı bug'ı hâlâ taşıyordu; 2026-09-01'in aynı gün ilerleyen bir merge'iyle main'in KENDİSİ oldu (main kendi title-bazlı `_close_windows_by_exact_title` fix'ini 2026-08-31'de bağımsız denemişti — X11/wmctrl'e bağımlıydı ve canlı doğrulanmamıştı, merge'de bu PID-bazlı+doğrulanmış versiyon lehine terk edildi). Orijinal teşhis (tarihsel bağlam): DONE.md'deki eski TODO-b fix'i (`kill_session_and_parent`'ı paylaşımlı `kill.py`'ye taşımak) NON-tmux mimari içindi; TBD#11 sonrası tmux-backed dal PID-ancestry'e hiç dokunmuyordu, sadece `tmux_kill_session(name)` çağırıyordu — ama spawn.py'nin penceresi `tmux new-session -A -s NAME ...; exec bash` olduğu (`-d` YOK, pencere tmux'un ATTACHED CLIENT'ının kendisi) için session ölünce attached client dönüp `exec bash`'e düşüyor, pencere kapanmıyordu.
- [ ] **`py/cops service install` unit-adları worktree-agnostic — hâlâ kod-seviyesinde bir footgun, ama 2026-09-01 merge'den sonra tetiklenme senaryosu (iki paralel canlı deploy) ortadan kalktı** (2026-09-01, yan bulgu) — `service.py`'deki `UNIT_NAMES`/`WEB_UNIT`/`TUNNEL_UNIT` hâlâ sabit (`claudeops-web.service`/`claudeops-tunnel.service`, worktree'den BAĞIMSIZ). react-ui bu commit'le main'e merge edildi + `feature/react-ui` worktree'sindeki paralel deploy (port 8766, `-react` suffix'li unit'ler) decommission edildi — yani şu an tek canlı deploy var, günlük kullanımda risk tetiklenmiyor. AMA `feature/react-ui` worktree'si diskte duruyor (silinmedi) — biri o worktree'den `py/cops service install` çalıştırırsa YİNE `~/.config/systemd/user/claudeops-web.service`'i (main'in canlı unit'i) EZER. Fix adayı (hâlâ geçerli): unit adını (+`--port`'u) `paths.REPO_DIR`'dan türet, `install`'a `--suffix`/`--port` bayrağı ekle; en azından `install` başında "bu isimde BAŞKA bir repo'ya ait bir unit zaten var" tespiti + onay iste.
- [ ] **Bulk handover: seçilen birkaç session'dan bazen sadece İLKİ başarılı oluyor, kalanı hataya düşüyor (main'de kısmen teşhis edildi, tam kök sebep AÇIK)** (2026-08-31, main worktree'de bulundu, bu merge'le taşındı) — Canlı tekrarlandı: 4 session seçilip bulk handover'a basıldı, sadece ilki (bu konuşmanın kendi session'ı) gerçekten kill+respawn oldu, diğer 3'ü hiç dokunulmadan hata döndü. İlk şüphe `guard_lock` ACQUIRE-timeout'unun (o zaman 5s) kendi TUTMA süresinden (kill+settle+spawn+wait_stable, worst-case ~45-50s) çok kısa olmasıydı — bu GERÇEK bir bug'dı ve ayrıca düzeltildi (bkz. `GUARD_LOCK_ACQUIRE_TIMEOUT=60.0`, zaten bu ağaçta mevcut), AMA main'in sonraki notu ("guard_lock fix confirmed PARTIAL, not full") bunun TEK sebep olmadığını gösteriyor — kalan kanıt iki `BrokenPipeError`, bilinen gecikmelerle uyuşmuyordu. Main'e geçici teşhis instrumentasyonu eklenmişti (`_handover`'ın her fazına `diag_log` + do_POST'un ortak yanıt-yazım noktasına client-koptu durumunu loglayan bir sarmalayıcı) ama bu ağaca (henüz PAGE_HTML/eski panel silinmeden önceki mimarisiyle yazılmıştı) taşınmadı — asıl mekanizma (guard_lock uzun süre tutuluyorken bulk-loop sıradaki item'a HEMEN geçiyor) burada da AYNEN geçerli, sadece do_POST/`_handover` dispatch şekli farklı. Bir dahaki bulk-ho'da tekrarlarsa: (a) benzer bir diag_log enstrümantasyonu bu ağacın `_handover`/do_POST'una ekle, (b) WebSocket broadcaster (bu ağaçta main'de YOKTU) bulk-action sonuçlarını HTTP yanıtından bağımsız push ediyor mu kontrol et — belki bu ağaçta zaten farklı bir yoldan çözülmüş olabilir, doğrulanmadı. **Güncelleme (2026-09-02, kod incelemesiyle DOĞRULANDI, canlı fleet'e dokunmadan):** (b) evet, zaten çözülmüş — `notify_status_changed()` sadece bir `_WAKE` event set ediyor, ayrı bir broadcaster thread'i `_status_payload()`'ı yeniden hesaplayıp `_WS_REGISTRY`'deki TÜM bağlı client'lara (hangi tab'ın hangi POST'u attığından bağımsız) push ediyor — yani tetikleyen HTTP yanıtı o an ölse bile açık diğer tab'lar birkaç saniye içinde doğru durumu görür. AMA gerçek bir gedik bulundu+kapatıldı: `_json()`'ın `self.wfile.write(body)`'i BrokenPipeError/ConnectionResetError/OSError fırlatınca istisna `_json_notify()` içindeki `notify_status_changed()` çağrısına HİÇ ULAŞMADAN do_POST'tan dışarı taşıyordu (aksiyon server-side BAŞARILI olsa bile) — artık `_json()` bu üç istisnayı yakalayıp `diag_log("response_write_failed", ...)` ile kayıt altına alıp yutuyor, `_json_notify` her zaman notify'a ulaşıyor. Ayrıca (a) da uygulandı: `_handover()`'ın her fazına (`handover_start/lock_acquired/killed/spawned/lock_timeout/reopen_failed/done`) `diag_log` eklendi — best-effort, canlı fleet'e dokunmuyor. **Hâlâ açık kalan:** bu SADECE gözlemlenebilirlik + bir yan-gedik fix'i — "sadece ilk item başarılı oluyor" belirtisinin ASIL mekanizması (guard_lock timeout fix'i sonrası) hâlâ canlı bulk-handover ile yeniden test edilmedi (bu oturumda bilerek yapılmadı — kullanıcı talimatı: düşük riskli/canlı-filoya dokunmayan fix'ler). Tekrarlarsa artık `diag.log`/Tanı sekmesi hangi fazda takıldığını gösterecek.
- [ ] **Terminal görünümünde bazı satırlar siyah kutunun sağ kenarını taşıyor (kozmetik)** (2026-08-28, [[terminal-view-refresh-destroys-xterm]] fix'i sırasında fark edildi) — okunabilirliği bozmuyor ama `computeFitFontSize`/`fitContainerToTerm`'in hesapladığı font-size ile gerçek container genişliği arasında küçük bir uyumsuzluk olabilir. Düzeltilmedi, düşük öncelik.
- [x] **web panel Terminal: CLI meşgulken (busy) ekran sürekli "blink" ediyor, alt komut kutusuna yazı bile giremiyor ✅ STALE-FIXED (react-ui rewrite)** (2026-08-28, kullanıcı: "web den bakilan terminal cok blink yapior txt ye komut bile yazilamiyor. Yazilinca blink sonrasi silinoyr. Sanirim calisinca oluyor bu. Update kesilince blink gidior"; ekran görüntüleriyle doğrulandı — `~/Pictures/Screenshots/Screenshot from 2026-08-28 08-40-4{9,53,58}.png`, modal `cops20260827_3`'ü "✻ Computing…" durumundayken gösteriyor) — orijinal teşhis eski `pollTerm` (`web.py` PAGE_HTML JS, ~2058) içindi: 200ms'de bir capture-pane metnini öncekiyle karşılaştırıp farklıysa TAM `term.reset()`+`term.write()` yapıyordu; CLI "Computing…" durumundayken spinner/süre/token-sayacı HER tick'te değiştiğinden bu neredeyse sürekli tetikleniyordu → flicker. **2026-09-02 doğrulandı (kod incelemesi, react-ui main'e merge sonrası):** o eski `pollTerm` kodu artık YOK — `TerminalView.tsx:214-216`'daki React karşılığı `result.text === inst.lastText && !resized` olduğunda reset+write'ı BÜTÜNÜYLE atlıyor ("skip reset+write entirely so quiet ticks... never visibly flicker" — dosyanın kendi yorumu), yani "Computing…" sırasında metin değişmediği sürece hiç yeniden çizim tetiklenmiyor. `inputText` de artık ayrı bir React `useState` (component'in kendi local state'i, poll'un hiç dokunmadığı) — "yazınca siliniyor" belirtisinin kök sebebi olan global-state-+ `innerHTML=`-wipe deseni yapısal olarak yok. Yeniden yazılmadı, sadece TODO kapatıldı.
- [x] **saseppr/agy: başlatma "açıldı" dedi ama pencere yok ✅ DONE** (2026-08-28) — `spawn.py`'ye retry (3 deneme) + tek-tık "pencere aç" butonu + son-15dk'da 2+ tam-fallback olursa panel-geneli uyarı banner'ı eklendi, canlı doğrulandı (`mo20260828`+`saseppr20260828_2` düzeltildi). Detay: DONE.md "spawn güvenilirliği" bölümü. Aşağıdaki analiz özgün teşhis süreci olarak (yanlış ilk tanı dahil) tutuluyor. ~~TEK SEFERLİK gnome-terminal-server flake'i (reboot sonrası TAZE server'da da tekrarladı), sessiz fallback kullanıcıyı gereksiz retry'a itiyor~~ (2026-08-28, kullanıcı: "saseppr hem claude baslattim onu gordum ama bir de ayni folder da agy actim actim dedi ama penceresini gormedim") — ⚠ İLK teşhisim (bu maddenin önceki hâli) YANLIŞTI, kullanıcı düzeltti: makine `08:49:53`'te restart edilmiş; gnome-terminal-server ŞU AN tek ve TAZE bir proc (pid 23582, `08:51`'de başlamış) — CLAUDE.md RFH'deki "pid 4693 hâlâ bozuk" durumu artık GEÇERSİZ (reboot temizledi), o yüzden "restart et" önerisi de geçersiz. Buna RAĞMEN aynı taze gt-server'la: boot-recovery'nin açtığı 5 session (cops/hc/mo/rustrino/trino, hepsi `08:52-53`) + `saseppr20260828` (claude, `08:59:36`) sorunsuz pencere aldı, ama `saseppr20260828_2` (agy, `09:07:50` — kullanıcının "actim" dediği) YİNE `spawn_fallback_used` ile windowless'e düştü (`diag.log` + canlı `wmctrl -l`/`tmux ls` ile doğrulandı: tmux session var ama "attached" değil, `wmctrl`'de pencere yok). Yani kök sebep kalıcı/eski bir bozukluk değil — taze serverda bile ARA SIRA olan tek seferlik bir flake (`spawn.py` yorumundaki "ARADA SIRADA olur" tam bu). Güncel durum (canlı doğrulandı, `~09:45`): `saseppr20260828_2` düzgün KAYITLI (`./cops list`: model gemini-3.1-pro-high, "dup yok") — ne orphan ne kayıtsız, sadece penceresiz/tmux-only çalışıyor (önceki "kayıtsız `agy-38204`" görünümü geçiciydi, RC-kaydı tamamlanınca düzeldi — bilinen [[handover-edge-cases]] "yavaş kayıt" ailesiyle aynı sınıf). Daha önce görülen `saseppr20260828_1` artık listede yok (ne olduğu netleşmedi, muhtemelen kullanıcı kapattı) — temizlik gerektiren bir dup KALMADI. Asıl kalan UX açığı: fallback SESSİZCE oluyor, kullanıcı anında fark etmiyor → tekrar deneyip gereksiz ikinci session açabiliyor (muhtemelen `_1`→`_2` geçişi tam bu yüzden oldu). Fix önerisi: spawn API yanıtına anında "⚠ pencere açılamadı, tmux-only modda başladı, Terminal butonundan eriş" uyarısı ekle (Tanı sekmesine gitmeye gerek kalmadan) — [[spawn-zombie-child-degrades-web-server]]/[[diag-tab-feature]] ailesiyle ilişkili ama "restart gt-server" bu spesifik flake'i ÇÖZMEZ (kanıtlandı), sadece anlık kullanıcı bildirimi çözer.
- [x] **mo/RC bridge geç kayıt → remote'da "açık değil" sanılıyor ✅ SÜPERSEDED (py tarafında LOKAL yüzey için yapısal olarak geçersiz)** (TODO-t, 2026-06-21 → 2026-09-04 triyaj) — mo53 açık+tek proc ama RC bridge dakikalarca `-` kaldı → kullanıcı remote'da (claude.ai) göremedi. **2026-09-04 doğrulaması:** py tarafı zaten TODO-k'nın kararıyla (başarı/durum kriteri = proc-presence, bridge-field DEĞİL — `handover.py`'nin kendi docstring'i "Proc-presence bekle" diyor) bridge-field'a HİÇ bakmıyor; `_status_payload()`/panel/list bridge kaydını beklemeden proc/cpu'ya göre anında doğru durumu gösteriyor — yani LOKAL panel için bu tuzak yapısal olarak yok (kod grep'i: `bridge` kelimesi geçen TÜM yerler ya kill-sonrası deregister-settle'la ilgili ya da yorum, aktif bir "bridge alanına göre karar ver" YOK). **Kalan tek yüzey** — claude.ai'nin KENDİ app'inin session'ı ne zaman göstereceği — bu kod tabanından kontrol edilemez, tamamen server-side bir gecikme; TODO-u (L15, "eski bridge birikimi") ile AYNI sınıf, orada takip ediliyor.
- [ ] **claude.ai'de ESKİ bridge birikimi → remote'da "duplike/açık değil" yanılgısı** (TODO-u, 2026-06-21, kullanıcı) — her handover claude.ai sunucusunda yeni RC bridge yaratıyor, eskiler silinmiyor. 5 nesil churn (*49→53) sonrası remote'da anomaly50/51/52/53 hepsi birikip "duplike CLI" gibi görünüyor; güncel olan kalabalıkta kayboluyor. LOKAL temiz (27/27 tek proc + RC). Kök: server-side bridge cache temizlenmiyor + aşırı churn. Fix: (a) churn'ü azalt (seyrek handover); (b) handover'da eski-suffix bridge'leri server'dan kapat/teardown (API varsa); (c) kullanıcı app'ten stale sohbetleri elle dismiss. İlgili [[handover-edge-cases]] server-side bridge cache.
- [x] **handover Faz 1'i gruplara böl / throttle — mass-Faz1 → sunucu rate-limit ✅ DONE (py/cops handover)** (TODO-v, 2026-06-21 → 2026-09-04 kapatıldı) — 24 session'a aynı anda `handover --force` → **sunucu rate-limit** ("Server is temporarily limiting requests", usage-limit DEĞİL) → 3 session (qve/emrgence/anomaly54) **blank-TUI hang** (jsonl son=`user`, terminal boş, jsonl'e error YAZILMAZ → tarama göremez, sadece TUI/remote banner). **Doğrulandı (2026-09-04, kod okuması):** `py/claudeops/handover.py`'nin Faz1 döngüsü tam istenen şekilde batch'li — `--batch-size` (varsayılan 5) + `--batch-delay` (varsayılan 30s) argümanları var, `i % batch_size == 0` olduğunda (dry-run hariç) `time.sleep(batch_delay)` ile bekliyor, docstring doğrudan `[[mass-faz1-ratelimit-stuck]]`'a referans veriyor. Stuck-detect ayrı bir komut olarak zaten var (`py/cops stuck` — "stuck session'ları tespit et: son=user + düşük CPU"). Bash'in eski `cmd_handover`'ı hâlâ bu throttle'a sahip değil ama TBD#12'nin açık sorusu (bash'in kaderi) — aktif kullanılan yüzey py tarafı zaten çözülmüştü, sadece işaretlenmemişti.

- [x] **cmd_handover başında reboot-check ✅ DONE (py/cops handover)** (TODO-p, 2026-06-19 → 2026-09-03 kapatıldı) — handover başlamadan önce boot zamanını kontrol et: makine ≤~30dk önce reboot olduysa **DUR + uyar**, `--force` ile baypas. Fix `commands/handover.py`'ye eklendi (bash'in `cmd_handover`'ına DEĞİL — o hâlâ TBD#12'nin açık sorusu, aktif kullanılan yüzey `py/cops handover`): `_seconds_since_boot()` (`/proc/uptime`'ın ilk alanı — `uptime -s` parse etmekten daha ucuz/sağlam, subprocess/locale riski yok) + `run()`'ın EN BAŞINDA (mesaj/guard.lock'tan bile önce) `REBOOT_GUARD_SECONDS=1800` kontrolü + yeni `--force` flag'i. Web panelinin tek-session "Handover" butonuna BİLEREK eklenmedi — o zaten kullanıcının bilinçli, panele bakarken attığı tek bir tık (bash TODO-p'nin asıl endişesi olan "gözetimsiz batch script'in boot-anında otomatik/toplu çalışması" riskini taşımıyor). **Doğrulandı:** gerçek makine uptime'ı (~6.3 gün) kontrolle guard'ı tetiklemediği + `_seconds_since_boot()` sahte-5dk değerine monkeypatch edilince `run()`'ın (a) `--force` YOKKEN `handover_faz1`'e hiç girmeden `rc=1` döndüğü (b) `--force` VARKEN guard'ı atlayıp normal akışa girdiği elle doğrulandı.
- [x] **Faz 1 reopen `--model`/`--permission-mode`/`--effort` GEÇMİYOR ✅ DONE (py/cops handover)** (TODO-q, 2026-06-19 → 2026-09-04 kapatıldı) — `cmd_handover` (bash) wrap-up reopen'ı `claude --resume <sid> -n <ad> --remote-control <ad> '<prompt>'` ile açıyordu; **model/permission/effort flag'leri YOK** → wrap-up session'ları varsayılan model + plan-mode'da açılıyordu. Doğrulandı 2026-06-19 hc51 wrap-up proc'unda flag yoktu. **Doğrulandı (2026-09-04, kod okuması):** py tarafının `_spawn_faz1()`'i (`py/claudeops/handover.py:166`) ÜÇÜNÜ de zaten geçiyor — `model=session.model or default_model_for(provider)`, `permission_mode=provider.permission_modes()[0]`, `effort=default_handover_effort(provider)` (bilerek `max` değil — [[handover-effort-high-not-max]] kararı, respawn edilen session'ın effort'u bir sonraki ho'ya kadarki TÜM ömrü için kalıcı varsayılan olduğundan). Bash'in `cmd_handover`'ı hâlâ eksik ama TBD#12'nin açık sorusu; aktif kullanılan py yüzeyi baştan beri doğruydu. **Not (2026-09-04, aynı gün sonradan):** `_spawn_faz1()` (yukarıda bahsedilen fonksiyon) Faz 1'in kill/respawn'dan canlı-enjeksiyona geçmesiyle TAMAMEN KALDIRILDI (bkz. DONE.md "2026-09-04 (4)") — bu madde hâlâ DOĞRU/kapalı kalıyor (o günkü haliyle py'nin gerçekten doğru yaptığını gösteriyordu), sadece referans verdiği fonksiyon artık yok; Faz 2 (`rc.py`, hâlâ gerçek spawn yapan tek yer) aynı `default_model_for`/`default_handover_effort` zincirini kullanmaya devam ediyor.
- [ ] **Handover öncesi guard cron'u DISABLE et + bekle** (TODO-r, 2026-06-19, kullanıcı) — guard.lock yarışından (TODO-j) daha kesin: `cmd_handover` başında guard cron'u geçici kapat (crontab comment / disable-flag dosyası), **birkaç saniye bekleyip guard'ın devreye GİRMEDİĞİNİ doğrula**, SONRA reopen'lara başla; handover bitince guard'ı geri aç. Bugün hc51 dup tam bu yüzden (guard Faz 1 ortasında respawn etti). TODO-j+TODO-r birlikte çözülmeli. **Not (2026-09-04, TODO-j'ninkiyle AYNI):** kod-seviyesinde madde hâlâ uygulanmadı, AMA pratik risk 2026-08-24'ten beri NÖTRALIZE — guard cron kalıcı KAPALI ([[feedback-manual-fleet-control]]), yani handover'la yarışacak otomatik bir guard artık yok. Guard cron ileride geri açılırsa TODO-j ile birlikte yeniden gerçek risk olur.

- [ ] **BUG: `rc <a,b,c>` virgül-separated isim listesi parse edilmiyor** — 2026-05-19'da 15→16, 2026-05-23'te 20→21 transition'larında doğrulandı. `cmd_rc`/`resolve_targets` SPACE-separated bekliyor. CLAUDE.md eski örnekler virgüllü idi → güncellendi. Fix: cmd_rc başında target ve "$@" içindeki virgüllü string'leri split et (IFS=','). Aynı bug `cmd_kill`, `cmd_compact`, `cmd_send` için de var.
- [ ] **`claudeops layout` orphan terminal kaldırmıyor** — 2026-05-19/05-23/05-25 transition'larında doğrulandı (ws1'de "fatihyuce@483-LNX: ~" bir quad slot işgal ediyor). Fix: layout iterasyonunda window-name'in geçerli session.json'da olup olmadığını kontrol et + yoksa skip (oth_wins'e ekleme).
- [ ] **`cancel` Esc güvenilmez (VTE reject)** — 2026-05-25 kulturiot23 "waiting" modal'da Esc inmedi. Garantili iptal = respawn. Fix: cancel Esc dene → 2s sonra hâlâ takılıysa otomatik `rc <name> --kill-first` öner/yap (flag ile).
- [x] **`handover --exclude=<name>` filtrelemiyor ✅ DONE** (2026-08-25) — bash exclude eşleşmesi artık base+suffix varyantlarını yakalıyor (`^(isim)[0-9]*(_[0-9]+)*$`); default exclude listesi de kaldırıldı (opsiyonel flag olarak duruyor). Tarihsel bağlam (2026-06-08) (*36→*37) `--exclude=anomaly` (base-name) verildi ama anomaly36 yine wrap-up edildi (pre-check sınıflandırmasında HO listesinde kaldı). Muhtemelen exclude suffix'li tam isim (`anomaly36`) bekliyor ya da parse hiç çalışmıyor. Fix: `cmd_handover` exclude eşleşmesini base-name + suffix'li ikisini de kabul edecek şekilde düzelt (handover hedef döngüsünde isim normalize).
- [ ] **KRİTİK (j kaldı): rc guard.lock'u KAYIT bitene kadar tutsun** (TODO-j, 2026-06-09 *37→*38) — ~~(h) `exec 9>guard.lock; flock 9` `cmd_rc` başında YAPILDI (2026-06-17, `93f99df`)~~. (j) hâlâ açık: spawn bitince lock serbest bırakılıyor ama RC bridge kaydı dakikalarca sürebiliyor → guard yarışıp dup açabiliyor. Fix: spawn'lardan sonra **beklenen N session `list`'te kayıtlı olana kadar poll** (timeout ~5dk), SONRA `flock -u`. Memory [[handover-edge-cases]] edge-9. **⚠ 2026-06-19: `cmd_handover` guard.lock'u HİÇ tutmuyor** (sadece `cmd_rc` tutuyor) → Faz 1 reopen sırasında guard yarışıp **hc51 dup açtı** (25s sonra --model'li respawn, aynı sid c111d2ba; elle temizlendi). Fix bunu da kapsamalı: `cmd_handover` reopen döngüsünü de `exec 9>guard.lock; flock 9` ile sar. **Not (2026-09-03):** kod-seviyesinde madde hâlâ uygulanmadı (spawn sonrası RC-kayıt hâlâ poll-doğrulanmıyor), AMA pratik risk 2026-08-24'ten beri NÖTRALIZE — guard cron kalıcı KAPALI ([[feedback-manual-fleet-control]]), yani lock'u yarışacak otomatik bir ikinci taraf artık yok. Guard cron ileride geri açılırsa bu madde yeniden gerçek risk olur.
- [x] **handover Faz1 başarı = PROC-varlığı (bridge değil) ✅ DONE** (TODO-k, 2026-06-21) — false-negative "bridge bulunamadı → failed=1" düzeltildi (hc53 testi: session AÇIK ama bridge geç kaydoldu → "failed" diyordu). Başarı kriteri artık **proc-presence** (`ps|grep -P '^claude' | grep --remote-control NAME(\s|$)`, DONE.md dersi: bridge-field gecikmeli); bridge sadece URL için bilgilendirici ("bridge-pending" if yok). Substring tuzağı yok (space/EOL anchor; hc53≠hcr53 doğrulandı). Sadece proc gerçekten yoksa "failed-noproc". (`cmd_handover` ~1489)
- [x] **rc/handover: spawn sonrası RC-kayıt yavaş → kaydı doğrula sonra dön ✅ SÜPERSEDED (aynı gerekçe, TODO-t'ye bkz.)** (TODO-l, 2026-06-09 → 2026-09-04 triyaj) — bash'in `rc`'sindeki `=== bridge check ===` adımı *38'de BOŞ dönmüştü. py tarafı bridge-check adımını hiç YAPMIYOR çünkü buna hiç ihtiyacı yok (TODO-t'de detaylandırıldığı gibi, başarı/liste kriteri proc-presence) — "poll'la doğrula" önerisi bash-mimarisine özgü bir ihtiyaçtı, py'de sorunun kendisi mimari olarak yok.
- [x] **cron/boot recovery artefakt-skip ✅ DONE** (`4f0543b` → DONE.md; opsiyonel `uptime` boot-anchor yapılmadı) (TODO-o) — `_latest_sid_for_cwd` (guard satır ~2013 + boot ~1864 kullanıyor) **en yeni mtime**'ı seçiyor. Reboot sonrası bu YANLIŞ: boot SONRASI oluşan boş/thin spawn-artefaktı (ör. ilk-user="session", 0 gerçek user mesajı) en yeni mtime'a sahip olup gerçek 808-satırlık pre-boot konuşmanın önüne geçiyor → session boş context'le açılıyor (2026-06-18 mo50: 1e6e54b7=808 satır orphan, 2d85e4b1=35 satır fresh açıldı). **Fix:** aday jsonl "artefakt" ise atla — artefakt = **hiç gerçek user turn'ü yok** (system-reminder + wrap-up promptu + tek-kelime "session" hariç substantive free-text user mesajı 0). Aday kalmazsa mevcut davranışa düş. Alternatif/ek: `uptime -s` boot-anchor — reboot-recovery'de sadece `mtime < boot` adayları al (steady-state guard'da post-boot işi resume etmeli, o yüzden boot-anchor SADECE boot-yakını pencerede). Kullanıcı kuralı (2026-06-18): "restart oldu → cron en son nerede ise ordan başlasın, ho olsun olmasın; restart-anındaki konuşma neyse o." ⚠ Steady-state'te (session mid-work crash) en son = post-boot olabilir → artefakt-skip boot-anchor'dan daha güvenli (ikisinde de çalışır). İlgili [[reboot-no-handover]].
- [x] **~~handover co + ulaksec'i base-name ile hard-exclude etsin~~ SÜPERSEDED** (TODO-n; 2026-08-25 kapatıldı) — isim-bazlı hariç-tutma TAMAMEN kaldırıldı (kullanıcı kararı: hedef seçimi web panel checkbox'larıyla); yerine process-bazlı self-koruma (`ancestor_pids()`, py handover+rc; bash `filter_not_self`). ulaksec artık kodla değil dikkatle korunuyor. Tarihsel bağlam (2026-06-18) — co + ulaksec models.tsv'de aktif (guard crash-recovery'de ayakta tutsun — İSTENEN davranış, kullanıcı 2026-06-18). AMA handover ASLA dokunmamalı. Eskiden co/ulaksec fleet'ten farklı suffix'te (co43 vs fleet49) olduğu için doğal atlanıyordu; guard die olunca onları **fleet suffix'ine bumplıyor** (co43→co50, ulaksec43→ulaksec50, çünkü guard `base+suffix` çalışmıyorsa resume eder). Artık `handover --from-suffix=50` co50+ulaksec50'yi eşler: co50 `filter_not_self` ile atlanır (self güvende) ama **ulaksec50 self değil → ho listesine girer = "dokunma" ihlali**. Fix: `cmd_handover`'da sabit `HO_EXCLUDE="co ulaksec"` base-name listesi, `filter_not_self`'ten sonra base-name (suffix-stripli) eşleşeni rows'tan düş. (TODO-d `--exclude` base-name parse'ıyla birleşebilir.) Kök sebep ayrıca: guard cwd-tespiti *50 cutover yoğunluğunda co43/ulaksec43'ü bir an ıskaladı → bumpladı.
- [ ] **handover `sid=-` → cwd-based jsonl fallback** (TODO-m, 2026-06-17) — `find_jsonl "$sid"` `sid=-` olunca boş dönüyor → `jsonl=no` → `needs_ho` SKIP → iş yapan yeni session'lar (sase49, marwan49 vb.) Faz 1'den atlıyor. Fix: `sid` boş/`-` ise cwd'den encode edilmiş proje path'ini türet → `~/.claude/projects/<encoded>/` altındaki en yeni `.jsonl`'ı kullan; encoding `add-session-to-fleet` kuralına uy (`/`→`-`, `_`→`-`). Etkilenen: `find_jsonl`, `needs_ho` sinyal tablosu, `handover_done`, `cmd_handover`'daki `--resume $sid` satırı.
- [x] **sase Faz1'de atlanıyor — needs_ho false-negative? (kontrol) ✅ KAPATILDI (moot — 2026-09-04 triyaj)** (2026-06-23, kullanıcı) — sase55 o günkü Faz1'de **skip** edilmişti (needs_ho=False: RFH+repo temiz+yeni commit yok), kullanıcı "sase'yi kaçırdık" demişti → sase'de kaydedilmemiş/commit'lenmemiş iş olabileceği şüphesi. Bu maddenin istediği "kontrol" (o anki sase reposunun gerçek git durumunu doğrula) bugün **artık yapılamaz** — o 2026-06-23 anlık-durumu üzerinden ~2.5 ay geçti, sase reposu o zamandan beri defalarca değişti, geriye dönük doğrulanacak bir şey kalmadı. `needs_ho`'nun genel mekanizması bu arada bağımsız olarak iki kez sertleşti (TODO-k: proc-presence kriteri; 2026-09-03: `has_real_user_turn` sıfır-mesaj fix'i) — ama bunların HİÇBİRİ bu spesifik incident'in kök nedenini (RFH-sonrası-clean-repo sezgisi mi yanılttı, yoksa gerçekten kaçırılmamış işti mi) doğrulamıyor/reddetmiyor. Kapatılıyor çünkü madde artık test edilemez, çözülmüş olduğu için değil — tekrar yaşanırsa (RFH var + repo temiz görünüyor ama kullanıcı "iş kaybettik" diyor) TAZE bir incident olarak yeniden aç.
- [ ] **layout title-bazlı eşleşme bayat başlığa KIRILGAN → PID ile eşle** (2026-06-23, kullanıcı "sase atlandı") — cross-suffix jsonl resume eden session'ın (rustrino55/sase55) TUI terminal başlığı eski suffix'te (*53) takılı kalıyor → `layout` pencere→session eşleşmesini **başlıktan** yaptığı için `known_names`(*55) ile eşleşmez → "claude değil" sayıp atlar (rustrino bir PIN'di, ws0'a gitmedi). Fix: `_list_windows`+`build_layout_plan` eşleşmeyi **PID** ile yapsın (`wmctrl -l -p` → window PID; ama gnome-terminal'de tüm pencereler server-PID paylaşır → alternatif: VTE pencere↔proc eşleşmesi başka yoldan, ör. `_NET_WM_PID` server olduğu için işe yaramaz; title fallback kalır). ⚠ **AÇIK:** rustrino55(0x0400682c)+sase55(0x0400a4c8) hâlâ elle yerleştirme bekliyor (`xdotool windowmove <wid>` ile; main layout 25/27 uygulandı). **Re-doğrulandı (2026-09-04, kod okuması):** çoklu-monitor pile-up fix'i (b2dcfd7, 2026-09-03) bu maddeye HİÇ dokunmadı — `py/claudeops/layout.py`'nin güncel hâlinde `_list_windows`/`_is_claude_window`/`_session_name_from_title`/`build_layout_plan` hâlâ SAF title-string eşleşmesi yapıyor (`known_names`'e tam eşleşme ya da `([a-z]+\d+)$` regex fallback), PID tabanlı bir eşleşme yolu yok — madde hâlâ tam olarak yazıldığı gibi geçerli, bash-dönemine özgü/stale değil.
- [ ] **resume cross-suffix jsonl → TUI başlığı BAYAT kalıyor** (2026-06-23) — recovery/Faz2 eski-suffix (*53) konuşmayı `--resume` ile *55 olarak açınca claude TUI terminal başlığını eski isimde (*53) bırakıyor (`-n *55` override'a rağmen, başlıkta aktif ✳ spinner var = canlı proc). Sonuç: title-bazlı TÜM araçlar (layout; muhtemelen remote görünüm) bu session'ları kaçırıyor. Araştır: claude başlığı `-n`'den mi jsonl session-meta'dan mı çekiyor → düzeltme spawn-side (resume'da title re-emit) mı tool-side (PID-eşleşme) mı. [[guard-cron-relative-path]] değil, ayrı.

- [ ] **config-corruption: eşzamanlı `~/.claude.json` yazması config'i BOZUYOR** (2026-06-13, [[config-corruption-resume-hang]]) — toplu handover/respawn'da onlarca claude config'e aynı anda yazınca **truncated JSON** → `claude --resume` startup'ta **BLANK-TUI hang** (fresh `--new` çalışır → yanıltıcı; teşhis `claude --resume <sid> -p x --debug`). Manuel fix: `~/.claude/backups/`'tan en yeni VALID backup'ı restore. **KOD FİX:** handover/rc respawn'larını **serialize et** (tek-tek, her birinden sonra bridge-kaydını bekle) + her adımda `python3 json.load(~/.claude.json)` config-check → bozulursa DUR + backups'tan auto-restore. (TODO-h+j lock-tutma ile birleşik.) ⚠ Done-tespiti: jsonl-stale **≥150s** (60s erken — session iş ortasında 60s+ duraklıyor → çakışma); `status` lag'liyor; en güvenilir done = kullanıcı-gözü. Ek tuzaklar: `import -window` (Mutter) stale-buffer YANILTICI; `ps|grep|kill`/`pkill -f` komut-metni session-adı içerince **kendi shell'ini ($$) öldürür** → `$$` hariç tut / PID ile.

- [x] **`needs_ho` sıfır-mesajlı session'ı skip etmeli ✅ DONE** (2026-06-25, 2026-09-03'te kapatıldı) — `--new` ile açılan boş/idle session'ların hiç substantive user turn'ü yok ama RFH da yok → `needs_ho=True` → `handover --dry-run` hepsini "WOULD handover" gösteriyor (yanlış pozitif). py'de hâlâ tam bu haliyle AÇIK olduğu doğrulandı (bash'e özel değilmiş). Fix: yeni `needs_ho.has_real_user_turn(jsonl_path)` — claude_provider.py'nin `_is_real_user_text` filtresiyle AYNI tanım (tool_result-only/sidechain/isMeta/boş metin SAYILMAZ), `needs_ho()`'un EN BAŞINA erken-çıkış olarak eklendi: jsonl varsa ama hiç gerçek user turn'ü yoksa cwd'nin git durumuna BAKMADAN doğrudan `False` (boş bir session'ın "wrap-up"a ihtiyacı olamaz — repo başka bir session/insan tarafından kirletilmiş olsa bile). Tek fonksiyon olduğu için hem `handover.py`'nin Faz1 batch döngüsü hem `web.py:751`'in panel "needs-ho" göstergesi otomatik kapsandı. **Doğrulandı:** 3 sentetik jsonl (boş/isMeta-only, gerçek-iş-RFH'siz, gerçek-iş-RFH'li) + temiz VE kasıtlı-kirli bir scratch git repo'suyla 5 senaryo elle test edildi — hepsi beklenen sonucu verdi (boş session dirty cwd'de bile artık `False`; mevcut RFH-tabanlı davranış value-for-value korundu).
- [x] **rc `--kill-first` sonrası biraz daha bekleme ✅ DONE (zaten var, py'de stale)** (2026-06-25 → 2026-09-03 kapatıldı) — kullanıcının istediği ek settle zaten mevcut: `rc.py --kill-settle` varsayılanı `3.0`s (`args.kill_settle`, sadece bash-notu "0" sanıyordu), web panelinin handover/compact yolu `HANDOVER_KILL_SETTLE_SECONDS = 6.0` kullanıyor — ikisi de kill-sonrası/spawn-öncesi ek bekleme. Bu madde bash döneminden kalma, py tarafında zaten çözülmüştü, sadece işaretlenmemişti.

## Geliştirme

- [x] **Terminal içi URL banner'ının yeri gözden geçirilsin ✅ DONE** (2026-09-02) — (2026-08-31, kullanıcı: "url listesi cmd line'a veya altta scrollu bir panel olabilir"; merge sırasında 2026-09-01'de yeniden bulundu — main'in `bba86f2` commit'inde kaydedilmişti ama iki bağımsız TODO.md arasında kayboldu, hiçbir tarafta uygulanmadı). Fix: `UrlBanner`, `TerminalView.tsx`'te xterm görünümünün ÜSTÜNDEN (her URL değişiminde tüm terminali aşağı kaydırıyordu) komut satırı girişinin HEMEN ÜSTÜNE taşındı (hint satırından sonra, `.opts` satırından önce) + kendi `maxHeight: 4.5rem` + `overflowY: auto`'suyla kaydırılabilir hale getirildi (kullanıcının iki önerisini de karşılıyor: cmd-line'a yakın VE scrollu). 3-URL cap'i (`extractTermUrls`) dokunulmadı — istek sadece konum/scroll hakkındaydı.
- [x] **web panel: listeden seçip session'lara "compact" gönderme ✅ DONE** (2026-09-02, kullanıcı: "terminal de ve listeden seçip onlara compact gonderme iyi olur") — bash'in `cmd_compact`'ının py/cops'ta hiçbir karşılığı yoktu (TOBEDECIDED#12'nin "compact"ı kapsamıyordu). Fix: yeni `_compact()` (`web.py`) — bash'le AYNI mekanizma: kill (LIVE session'la eşzamanlı `/compact` çalıştırmamak için, [[claude-2183-conversation-truncation]] riski) → headless `claude --resume <sid> -p '/compact' < /dev/null` (interaktif `--resume ... 'PROMPT'` ile `/compact`'ı ilk mesaj olarak göndermek YERİNE bilerek bu — TUI'nin CLI-argümanı bir ilk mesajı slash-command olarak mı yoksa düz metin olarak mı işlediği doğrulanmadı, headless `-p` ise bash'te KANITLI) → aynı geçmişle resume. guard_lock kill VE respawn için AYRI kısa pencerelerde tutuluyor (headless adım dakikalarca sürebilir, tek pencereye alınca `GUARD_LOCK_ACQUIRE_TIMEOUT=60s` diğer TÜM panel aksiyonlarını kilitlerdi — guard cron zaten kapalı olduğu için iki pencere arası teorik risk bugün yok). Şimdilik SADECE `claude` provider'ı destekliyor (`/compact` claude-cli'a özgü). BulkBar'a 5. aksiyon olarak eklendi (Çalışanlar sekmesi). **Canlı doğrulandı** (throwaway scratch-dizin test session'ı, gerçek fleet'e dokunulmadan): headless `-p '/compact'` çalıştırıldı, jsonl'de `isCompactSummary:true` sayısı 0→1 çıktı (exit 0) — mekanizma gerçekten çalışıyor. HTTP route'u da scratch bir web instance'ında (farklı port) test edildi (`/api/compact` doğru `_compact()`'a dispatch ediyor, geçersiz isimde doğru "çalışmıyor" hatası dönüyor). **Güncelleme (2026-09-04): mekanizma TAMAMEN DEĞİŞTİ** — kill+headless+respawn (yukarıdaki tüm açıklama artık TARİHSEL) canlı `tmux_send_keys` enjeksiyonuyla değiştirildi, kill/pencere-kapatma/respawn'ın HİÇBİRİ kalmadı. Detay: DONE.md "2026-09-04 (3)".
- [x] **Handover Faz 1: kill/respawn/pencere-kapatma-açma TAMAMEN KALDIRILDI — artık CANLI mesaj gönderimi ✅ DONE** (2026-09-04, kullanıcı: "ho için ama fazladan bir ekran açıyorsun. kullanıcı karar versin ne zaman kapatması ne zaman açmasına. sadece ho gonder yeter bence") — hem `py/claudeops/handover.py`'nin batch/CLI `handover_faz1()`'i hem `web.py`'nin tek-session `_handover()`'ı (panelin Handover butonu) `_spawn_faz1()`/`kill_session_and_parent`/`spawn_session`/`guard_lock`/proc-presence-bekleme'nin HEPSİNİ kaldırıp `tmux_send_keys(name, message)` ile mesajı DOĞRUDAN çalışan session'a enjekte edecek şekilde yeniden yazıldı — session hiç ölmüyor, pencere hiç kapanmıyor/açılmıyor. Faz 2/3 (fresh session açma / layout) artık Faz 1'in otomatik devamı DEĞİL, kullanıcının kendi ayrı kararı (CLAUDE.md'nin Handover bölümü güncellendi). `commands/handover.py` (CLI): `--proc-wait`/`--grace`/`--kill-settle`/`--display` flag'leri (hepsi artık anlamsız) kaldırıldı, `guard_lock` sarmalayıcısı kaldırıldı (kill-respawn arası "session yok" penceresi artık yok), hardcoded-isim-listeli "Sonraki adım: Faz 2..." otomatik önerisi kaldırıldı (o da bu "kullanıcı karar versin" ilkesine aykırıydı). `Faz1Summary.opened` → `.sent` olarak yeniden adlandırıldı (artık pencere "açmıyor", mesaj "gönderiyor"). **Canlı doğrulandı (throwaway session'lar, gerçek fleet'e dokunulmadan):** (1) idle bir session'a gerçek `web._handover()` çağrısı → AYNI tmux session/pencere hayatta kaldı, wrap-up mesajı işlendi, GERÇEK bir git commit + CLAUDE.md'ye "READY FOR HANDOVER" özeti eklendi (sahte/scratch bir repo'da uçtan uca); (2) AYNI session'a `handover.handover_faz1(names=[...])` (batch/CLI yolu) ile ikinci bir test mesajı → doğru şekilde alındı, `status="sent"` doğru kaydedildi. **Yan bulgu (önemli, ayrı bir açık TODO'yu da çözüyor):** bu testler sırasında `HANDOVER_MSG_DEFAULT`'ın TAM içeriği (1204 karakter, çok satırlı, Türkçe karakterli) `tmux_send_keys()`'in mevcut `-l`+ayrı-`Enter` mekanizmasıyla gönderildi ve TEK bir tur olarak doğru iletildi — açık "multiline paste" TODO'sunun varsaydığı erken-gönderim riski (en azından claude v2.1.260'da) GERÇEK DEĞİLMİŞ, o madde de bu bulguyla güncellendi. Detay: DONE.md "2026-09-04 (4)".
- [x] **Handover: wrap-up öncesi Sonnet'e geçici düşür, sonrasında eski modele dön ✅ DONE (claude provider)** (2026-09-04, kullanıcı: "handover komutu oncesi model sonet e vebsonrası eski modele. secili model dahandusukse kalsın") — wrap-up mekanik bir iş (commit/doc güncelleme), pahalı bir modele gerek yok. Yeni `CliProvider.handover_model_downgrade(current_model)` (varsayılan None — sadece claude override eder) + `apply_live_model_switch(tmux_name, target)` (varsayılan no-op) — `last_exchange`/`compact_command` ile AYNI "None=yok/dokunma" deseni. Hem `handover_faz1()` (batch/CLI) hem `web.py`'nin `_handover()`'ı (tek-session) simetrik olarak kullanıyor: wrap-up mesajından ÖNCE `/model sonnet`'e geçici düş, mesajdan SONRA orijinal modele geri dön.
  - **Sıralama (canlı doğrulanan `/model` seçici metniyle):** Fable ("most capable for your hardest and longest-running tasks") ve Opus ("best for everyday, complex tasks") İKİSİ de Sonnet'ten ("efficient for routine tasks") YUKARIDA sayılıp geçici düşürülüyor; Haiku ve zaten-Sonnet olan session'lar (kullanıcının "daha düşükse kalsın" isteği) HİÇ dokunulmadan bırakılıyor; tanınmayan/boş bir model adı da GÜVENLİ VARSAYILANLA (dokunma) ele alınıyor.
  - **Canlı test SIRASINDA 2 GERÇEK bug bulunup düzeltildi (kritik, kayda değer):**
    1. **Mesaj kaybı:** `/model X` bazen (ne zaman olduğu ÖNCEDEN KESTİRİLEMEZ, aynı "meşgul" durumda bile tutarsız) bir "Switch model?" onay diyaloğu açıyor. İlk NAİF implementasyon (gönder + sabit kısa bekleme + kayıtsız-şartsız Enter) canlı testte dialog TAM o bekleme penceresinin DIŞINDA açılınca hemen ardından gönderilen wrap-up mesajının dialog'a yazılıp SESSİZCE KAYBOLMASINA yol açtı. Fix: `apply_live_model_switch` artık pane'i POLL'layıp dialog'un GERÇEKTEN açıldığını doğruluyor, sadece o zaman Enter gönderiyor (20s timeout + son bir "kör" Enter denemesi).
    2. **Klavye-seviyesi çakışma:** `tmux_send_keys()`'in DÖNMESİ, hedef CLI'nin mesajı TAM işlediği an DEĞİL — sadece tuşların tmux'a teslim edildiği an. Wrap-up mesajından (1000+ karakter) HEMEN sonra restore komutunu göndermek, komutun HÂLÂ işlenmekte olan mesajın input kutusuna karışmasına (ne mesaja temiz eklenip ne de komut olarak çalışıp SESSİZCE kaybolmasına) yol açtı — canlı doğrulandı (session Sonnet'te TAKILI kaldı). Fix: yeni `POST_MESSAGE_SETTLE_SECONDS=2.0` — wrap-up mesajı gönderildikten SONRA, restore denemesinden ÖNCE kısa bir yerleşme süresi.
  - **Bilinen KALAN sınır (kabul edilebilir bulundu, dürüstçe belgelendi):** dialog'un açılması session'ın O ANKİ turu bitirmesine bağlı — bir wrap-up turu 20s'den UZUN sürerse (canlı testte tek-bir-uzun-metin-üretimi senaryosunda 1m42s'e kadar görüldü, ama GERÇEKÇİ çok-adımlı bir wrap-up — git/dosya komutları arasında doğal boşluklar olduğu için — 20s içinde temiz şekilde tamamlandı) dialog fonksiyon döndükten SONRA açılıp ONAYSIZ kalabilir. Bu ZARARSIZ: dialog SONSUZA kadar geçerli kalıyor (canlı doğrulandı — dakikalar sonra gönderilen tek bir Enter bile temiz onaylıyor), hiçbir şeyi bozmuyor, sadece o session bir sonraki etkileşime kadar bekleyen bir dialog gösterebilir.
  - **Canlı doğrulandı (throwaway session'lar, `--remote-control` DAHİL doğru spawn edilerek — ilk denemede bunu unutup `model` alanının hep `None` döndüğünü fark ettim, kendi test kurulumumun hatasıydı, kod değil):** idle bir Opus session'da gerçek `web._handover()` çağrıldı → `/model sonnet` (temiz, dialog yok) → TAM wrap-up mesajı bozulmadan iletildi VE gerçekten işlendi (repo'ya baktı, "READY FOR HANDOVER" yazdı) → `/model claude-opus-5` (temiz, dialog yok) → `/model` seçicisinin ✔ işaretiyle DOĞRULANDI: session doğru şekilde Opus'a geri döndü.
  - Doğrulama: `python3 -m py_compile` (4 değişen dosya, temiz) + yukarıdaki canlı E2E test.
- [ ] **web panel Terminal: çok-satırlı (multiline) prompt gönderme** (2026-09-03, kullanıcı) — komut kutusu şu an `<input type="text">` (`TerminalView.tsx:354`), native HTML text input — `\n` HİÇ içeremez (Enter'ı `handleSend()`'e bağlıyor, satır sonu tuşu yok). SADECE `<input>`'ü `<textarea>` yapıp Enter→gönder/Shift+Enter→yeni-satır eklemek YETİYORMUŞ — orijinal endişe (backend'in `tmux_send_keys()`'i `send-keys -l TEXT` + AYRI `send-keys Enter` kullandığı için gömülü `\n`'lerin pty'ye erken-Enter gibi ulaşıp mesajı ORTADAN böleceği) **2026-09-04'te CANLI YANLIŞ ÇIKTI** (handover/compact rewrite'ı sırasında, `[[claude-2183-conversation-truncation]]` DEĞİL bu maddeyle aynı mekanizma test edilirken bulundu): throwaway bir session'da HEM sentetik 3 satırlı bir mesaj HEM DE `HANDOVER_MSG_DEFAULT`'ın TAM kendisi (1204 karakter, çok satırlı, Türkçe karakterli, madde işaretli, box-drawing unicode satırlı — `\n\n` dahil) mevcut `tmux_send_keys()` ile (paste-buffer'a hiç geçmeden) gönderildi — ikisi de CLI tarafında TEK bir kullanıcı turu olarak doğru alındı, satır satır erken-gönderim YOK. Yani **paste-buffer/bracketed-paste aşaması muhtemelen gereksiz** (en azından `claude` v2.1.260 için) — kalan tek iş: (1) bu bulguyu agy/codex/gemini için de doğrula (sadece claude'da test edildi, "CLI'lar sabit durmuyor" ilkesi hâlâ geçerli — farklı CLI'ların TUI'leri farklı davranabilir); (2) doğrulanırsa doğrudan frontend `<textarea>`'ya geç (Enter=gönder, Shift+Enter=yeni satır — mevcut tek-satır `onKeyDown` davranışını KORU, sadece Shift+Enter ekle), backend'e YENİ BİR ŞEY EKLEMEDEN (mevcut `tmux_send_keys()` yeterli). Detay/kanıt: DONE.md "2026-09-04 (4)".
- [ ] **web panel: "ho+new cli" birleşik bulk aksiyonu** (2026-09-02, kullanıcı: "ho+new cli ozelliği aynı anda hem ho yapacak başarılı biterse new cli açacak başarılı bitmeyenleri bırakacak") — seçili session'lara handover UYGULA, HER BİRİ İÇİN AYRI AYRI: handover başarılı biterse ARDINDAN o aynı base isimle bir "yeni chat" (mevcut `apiNewChat`/"+ yeni chat aç" akışıyla AYNI — ayrı, ek bir session, mevcut olana dokunmadan) da başlatılsın; handover başarısız olan isimler İÇİN yeni chat adımı ATLANSIN (o isimler olduğu gibi bırakılır, elle müdahale gerekir). `compact`'ın az yukarıdaki eklenişiyle AYNI aile (BulkBar'a yeni bir aksiyon) ama compact'tan farklı olarak İKİ AYRI backend çağrısını (handover, sonra new-chat) ZİNCİRLEMESİ gerekiyor — muhtemelen frontend'de (`BulkBar.tsx`'in per-isim döngüsünde `apiHandover` sonucu `ok` ise ardından `apiNewChat({base: name, ...})`) yeni bir backend route'una gerek kalmadan uygulanabilir. Netleşmesi gereken detaylar (implementasyona geçmeden önce): yeni açılan session'ın ismi (muhtemelen mevcut "yeni chat"ın otomatik isim üretimiyle aynı, `{base}{tarih}` + çakışırsa `_1,_2`); yeni chat'in model/permission-mode/effort'u neye göre seçilecek (mevcut `default_model` Settings'i mi, yoksa handover edilen session'ın kendi ayarları mı); UI'de tek bir buton mu yoksa handover butonunun yanına bir "+ yeni de aç" seçeneği/checkbox mı.
- [ ] **Yeni session'ların hangi masaüstüne gideceğini ayarlama + otomatik dağıtım** (2026-09-02, kullanıcı: "dekstoplar ve cli ların gideceği yerleri set etme yolsa default yayma olsa iyi olur") — bash'in `desktops` komutunun (N masaüstü sabitle) py/cops'ta karşılığı yok (TOBEDECIDED#12). Şu an `py/cops layout`/React Layout sekmesi TEK-SEFERLİK, elle tetiklenen bir "var olan pencereleri yerleştir" aksiyonu (`--pin=`/`--group=`) — spawn ANINDA "bu isim hep ws2'ye gitsin" ya da "yeni session'lar masaüstlerine otomatik/sırayla dağılsın" gibi KALICI bir davranış yok. Yukarıdaki HDMI/çoklu-monitor bug'ı 2026-09-03'te düzeldi — bu artık bloke değil, istenirse üstüne inşa edilebilir.
- [ ] **Terminal'den CLI'a serbest metin yerine yapılandırılmış komut gönderme (komut paleti)** (2026-09-02, kullanıcı: "metin gonderioz ama kkmut da gönderebilioz mu? hangi cli providersa onun komıt listesşni seçip gönderme iyi olurdu"; kullanıcı: "ama acil değil") — şu an terminal input'u serbest metin yazıp gönderiyor (slash-command'lar da elle yazılarak gönderilebilir, ör. `/compact`), ama CLI'nın kendi desteklediği komutların listesini görüp seçerek göndermek yok. Provider'a `slash_commands()` gibi bir method eklenip TerminalView'e bir seçim/palet UI'ı eklenebilir — düşük öncelik.
- [ ] **(a) "Makine durumu" sekmesi (host-seviyeli CPU/RAM/disk/uptime)** (2026-09-02, kullanıcı) — Tanı sekmesindeki mevcut web/gt-server proc bilgisinden FARKLI, makinenin kendisi hakkında. Henüz uygulanmadı, düşük öncelik.
- [x] **(b) Web'den remote desktop — "Uzak Masaüstü" sekmesi ✅ v1 DONE (view-only)** (2026-09-02 istek → 2026-09-04 uygulandı, kullanıcı: "bir yeni tab da remote desktoplar için çalışmaya başlayalım. gerekirse rust kodu yazalım") — orijinal istek "hazır bir araç kullanılsın (VNC/noVNC), sıfırdan yazmaya gerek yok" diyordu, ama bu makinede x11vnc/tigervnc/websockify/xrdp/wayvnc'nin HİÇBİRİ kurulu değildi VE panelin tek-portlu Cloudflare quick-tunnel'ı (named tunnel yok, [[tunnel-no-named-tunnel-autoupdate-rotates-url]]) ham VNC/RDP protokolünü taşıyamaz (sadece HTTP/WS) — kullanıcı ayrıca Rust'a açık olduğunu belirtince mimari **WebSocket-native custom bir daemon**a döndü (noVNC/websockify hiç gerekmedi).
  - **Mimari:** yeni `rust/screenshare/` (Rust binary crate, `x11rb`+`tungstenite`+`image`) — X11 `GetImage` ile root window'u (birleşik sanal ekran) periyodik yakalar (2 fps), JPEG'e encode eder (kalite 70), `127.0.0.1`'e bind olan kendi WebSocket sunucusundan binary frame olarak akıtır. **`xcap` (cross-platform, daha kolay bir crate) ÖNCE denendi ama build-time bu makinede kurulu olmayan `libpipewire-0.3` dev paketini ZORUNLU kılıyordu** (Wayland/PipeWire portal desteği yüzünden — bu makine X11, o desteğe hiç ihtiyaç yok); `x11rb`'ye geçilince SIFIR ek sistem paketi gerekti (pure-Rust X11 protokol implementasyonu).
  - **Auth:** daemon'ın KENDİSİ auth yapmıyor — sadece localhost'a bind, dışarıdan erişilemez. Yeni `py/claudeops/remote_desktop.py` (spawn/kill/status lifecycle — binary'yi `cargo run` ile SARMALAMADAN, `cargo build --release` + `cargo metadata`'dan çözülen gerçek binary yolu doğrudan `Popen`'lanıyor, `.terminate()`'in gerçek process'e ulaşacağı GARANTİ olsun diye) + `web.py`'ye yeni `_proxy_desktop_ws()` — mevcut token kontrolünden (`_authorized()`) GEÇMİŞ bir `/ws/desktop` isteğini ham bayt düzeyinde (`web_ws.py`'nin JSON-broadcast WS'inden TAMAMEN AYRI, WS framing'e hiç dokunmadan) daemon'a proxy'liyor.
  - **View-only v1 (bilinçli kapsam):** mouse/klavye enjeksiyonu YOK — `x11rb`'nin `xtest` feature'ı ekli ama kullanılmıyor, ayrı/daha riskli bir fast-follow (kilitli bir ekranın GÖRÜNTÜSÜNÜ almanın zararsız olduğu canlı doğrulandı — X11 `GetImage` ekran kilidinden ETKİLENMİYOR — ama input enjekte etmek fiilen kilit ekranını uzaktan açabilmek demek, [[layout-needs-unlocked-screen]] ailesiyle akraba ama TERSİ yönde bir risk, daha dikkatli tasarım ister).
  - **Frontend:** yeni `DesktopTab.tsx` — Başlat/Durdur butonları + `<img src={blob URL}>` ile frame render (canvas'a gerek yok, ~2fps'te fark etmiyor); yeni `TabKey="desktop"`, TabBar'da daemon çalışırken "●" göstergesi (diğer tab/cihazlardan da görünsün diye `remote_desktop.status()` `_status_payload()`'a eklendi, WS push ile canlı yansıyor).
  - **Canlı doğrulandı (gerçek fleet'e dokunulmadan, scratch bir web instance'ında farklı portta):** (1) X11 capture tek başına (`cargo run` ile POC) — saved PNG Read tool'la GÖRSEL doğrulandı, renkler doğru (BGRX→RGB dönüşümü doğru); (2) Rust WS sunucusu tek başına — gerçek bir Python `websockets` client'ı ile 3 frame alındı, JPEG'ler görsel doğrulandı; (3) TAM ZİNCİR (`/api/desktop/start` → gerçek proc spawn → `/ws/desktop?token=...` → proxy → gerçek frame'ler) — token'lı gerçek HTTP/WS istekleriyle uçtan uca test edildi, ekran görüntüsünde makinenin GERÇEK canlı masaüstü (4 gerçek fleet terminali + dosya yöneticisi) doğru şekilde göründü; (4) `stop()` sonrası process'in GERÇEKTEN öldüğü (orphan kalmadığı) doğrulandı. `tsc`/`oxlint` temiz + Vite dev server (`npm run build` DEĞİL) yeni bileşeni hatasız transform ediyor.
  - **Doğrulanamayan (araç kısıtı):** gerçek bir tarayıcıda tıklayarak/görsel olarak test edilmedi — bu ortamda browser-automation aracı yok; backend zincirinin (asıl riskli/karmaşık kısım) tamamı canlı doğrulandı, sadece React bileşeninin tarayıcı-içi çalışma zamanı davranışı (WebSocket/blob URL/render) doğrulanamadı.
  - **Kalan/fast-follow:** mouse/klavye kontrolü (aşağıdaki AÇIK madde — YARIM kaldı), tek-monitör seçimi (şu an hep BİRLEŞİK sanal ekranı gösteriyor), bant genişliği/kalite ayarı, uzun süre açık kalırsa otomatik durdurma (şu an sadece elle Durdur var — sürekli ekran yakalayan bir daemon'ın unutulmuş açık kalması gerçek bir risk).
- [ ] **Uzak Masaüstü v2: mouse/klavye/mobil kontrol — RUST TARAFI YAZILDI, HENÜZ CANLI DOĞRULANMADI (kullanıcı "sonra yapalım" dedi, bilerek yarıda bırakıldı)** (2026-09-04, kullanıcı: "now remote desktop user input mouse keybord etc mobile handwrite") — `rust/screenshare/src/main.rs`'e `enigo` crate'i (v0.6.1) eklendi — hand-rolled XTest keycode-remap yerine (Türkçe karakterler ğ/ü/ş/ı/ö/ç için ŞART, muhtemelen şu anki X keymap'te YOK) `enigo`'nun kendi Linux/X11 backend'i (xkbcommon-tabanlı) kullanılıyor, `Keyboard::text()`/`Key::Unicode` ile keyfi Unicode + `Mouse::move_mouse`/`button`/`scroll` ile mouse. Bağlantı artık BİDİRECTIONAL: tek thread'te kısa bir `read_timeout` (50ms) ile "gelen input var mı, yoksa sıradaki frame zamanı mı geldi" poll döngüsü — mutex'li iki-thread yaklaşımı YERİNE (reader thread `ws.read()`'de bloklarken frame-gönderen thread'i kilitlerdi). JSON mesaj protokolü (`{"t":"move"/"down"/"up"/"scroll"/"key"/"text", ...}`) `serde`/`serde_json` ile parse ediliyor. **Derledi (`cargo build --release` temiz), AMA input enjeksiyonu HİÇ canlı test edilmedi** — kullanıcı test için açtığım izole pencereyi (`INPUTTEST_SAFE_TARGET`) görünce "sonra yapalım" dedi, ben de mouse/klavye göndermeden DURDUM (sadece bir ekran görüntüsü aldım, o da temizlendi). Python proxy (`_proxy_desktop_ws`) tarafında HİÇBİR değişiklik gerekmiyor (zaten ham-bayt/protokol-agnostik iki-yönlü pompalıyor — tasarımın bu esnekliği burada işe yaradı). **Frontend'de HİÇ iş yapılmadı** (mouse/touch/keyboard event capture, koordinat ölçekleme `naturalWidth/clientWidth` ile, mobil sanal klavyeyi tetiklemek için gizli bir `<input>`, ve önemli bir GÜVENLİK/UX detayı: yanlışlıkla dokunma/tıklamayı önlemek için muhtemelen bir "Kontrolü Al" açık-kapa anahtarı — hiçbiri henüz tasarlanmadı bile). ⚠ **Ayrı bir yan olay:** bu araştırma sırasında `pkill -f "screenshare 8877"` ile YANLIŞLIKLA CANLI (production, kullanıcının panelde kullandığı) daemon'ı öldürdüm — hemen fark edilip `remote_desktop.stop()`+`.start()` ile GERÇEK API üzerinden düzeltildi, kullanıcıya anında bildirildi. Ders CLAUDE.md'ye eklendi (aşağıya bkz.). **Sonraki oturum için net görev sırası:** (1) Rust tarafını izole/güvenli bir test hedefiyle (yeni bir pencere, tam geometrisi doğrulanmış) canlı doğrula — mouse move/click, klavye (Türkçe karakterler DAHİL), scroll; (2) frontend event capture + koordinat ölçekleme + "Kontrolü Al" güvenlik anahtarı; (3) uçtan uca test; (4) commit/deploy kullanıcı onayıyla.
- [ ] **Provider mimarisine yeni CLI backend'ler (chatgpt/codex, deepseek, gemini, vb.)** (2026-08-30, kullanıcı: "chatgpt cli, deepseek cli etc başka neler var eklenmesi gereken? sence") — mimari zaten hazır (`CliProvider` + registry, bkz. `shell_provider.py`), yeni bir backend eklemek bir provider dosyası yazmaktan ibaret. ⚠ Bu liste 2026-01 model bilgisiyle + bu makinede canlı kontrolle yazıldı, 8 aylık boşluk var — agy'de olduğu gibi CLI yüzeyleri hızlı değişiyor, EKLEMEDEN ÖNCE mutlaka canlı `--help` doğrulaması şart (TOBEDECIDED#10'daki agy dersi: "CLI'lar sabit durmuyor").
  1. **`gemini` (Google Gemini CLI) — EN YÜKSEK ÖNCELİK, bu makinede ZATEN KURULU** (`~/.nvm/.../bin/gemini`, v0.49.0) — `agy`'den (Antigravity) FARKLI, ayrı bir ikili; canlı `--help`'te doğrulandı: `-m/--model`, `--approval-mode default|auto_edit|yolo|plan` (permission_mode'a temiz eşlenir), `-r/--resume [latest|N]`, `--session-id` (isimlendirme için `--remote-control`/`COPS_NAME`'e üçüncü bir alternatif olabilir — env yerine doğrudan cmdline argümanı, agy'den daha kolay), `-p/--prompt` (headless). Sıfır kurulum maliyeti + zaten en somut/hazır aday.
  2. **OpenAI Codex CLI** (`codex` binary) — "chatgpt cli" muhtemelen bunu kastediyor (OpenAI'ın resmi terminal coding agent'ı); bu makinede kurulu değil, gerçek flag'leri doğrulanmadı.
  3. **DeepSeek:** resmi/ayrı bir "DeepSeek CLI" agent ürünü olduğundan emin değilim (varsa adı/olgunluğu doğrulanmalı). Daha olası pratik yol: **Aider** (model-agnostic, açık kaynak, DeepSeek API'sini YERLEŞİK destekliyor) — DeepSeek'e özel bir provider yazmak yerine Aider'ı tek bir provider olarak eklemek muhtemelen daha az iş + bonus olarak başka modelleri de (OpenAI, yerel Ollama, ...) aynı provider'dan kapsar.
  4. Daha düşük güvenle/araştırılmadı: Cursor'ın CLI agent modu, Amazon Q Developer CLI, GitHub Copilot CLI (`gh copilot` — muhtemelen tek-seferlik öneri modunda, claude/agy/gemini gibi kalıcı-interaktif-session değil, bu mimariye uyup uymadığı şüpheli).
- **Öneri:** `gemini`'den başla (kurulu + doğrulanmış + en az sürtünme), ikinci sıra Codex ya da Aider. Her yeni provider agy entegrasyonundaki gibi canlı `--help` + gerçek bir spawn/discovery testiyle doğrulanmalı, tasarım notuna güvenilmemeli.

- [ ] **roster'daki tarih-isimli çöp kayıtları temizle** (2026-08-25) — `rustrino20260825`/`rustrino20260825_1`, `line20260825`, `trino20260824` gibi test/ad-hoc satırlar roster+models.tsv'de birikti; base regex `_N` indirgemesi sonrası bunlar "Kayıtlı" sekmesinde durmuş-gibi görünüyor. Hangi isimlerin kalıcı olacağı kullanıcı kararı — sorup sil/birleştir.
- [x] **web panel: `config_msg` dil-duyarlı değil ✅ DONE** (2026-09-02) — (2026-08-26) özet satırındaki "config: ~/.claude.json geçerli" metni panel EN'deyken de Türkçe (validate_config `lang` almıyordu). Fix: `validate_config()` artık hazır bir cümle değil dil-nötr `(ok, code, detail)` döndürüyor (`code` ∈ valid/not_found/corrupt/unreadable) — panelin geri kalanıyla aynı desen (backend ham veri, frontend `t.configMsg(code, detail)` ile yerelleştirir). `Banners.tsx`/`App.tsx` güncellendi. CLI `py/cops config` (`commands/config.py`) de aynı 3-tuple'a geçirildi (aksi halde `ok, msg = validate_config()` unpacking'i ValueError ile PATLARDI — ayrı bir tüketici olduğu grep'le fark edildi).
- [ ] **web panel: kayıtlı proje için "yeniden adlandır" (rename)** (2026-08-26, kullanıcı isteği) — sase→saseppr rename'i elle TSV düzenlemeyle yapıldı; UI'de karşılığı yok. Tasarım: durmuş satırda basit rename (roster+models satır adı değişir); çalışan satırda adopt-benzeri akış (kill → yeni isimle `--resume` respawn → TSV güncelle). `_register_project`'teki isim-çakışma kontrolleri (roster + çalışan-base) aynen uygulanmalı.
- [x] **web panel: ulaksec satırına "dokunma" uyarı rozeti (opsiyonel) ✅ DONE** (2026-09-02) — (2026-08-25) isim-bazlı kod koruması kalktı; toplu seçimde yanlışlıkla işaretlenmesin diye satırda küçük bir uyarı işareti/onay iyi olur. Fix: `components/shared/protectedNames.ts` (`isProtectedName`, `/^ulaksec(?:\d.*)?$/`) + Running/Registered satırlarında (`SessionRow.tsx`/`RegisteredTab.tsx`) isim yanında amber "dikkat/caution" rozeti (mevcut `.unreg-badge` stiliyle). SADECE görsel uyarı — hiçbir seçim/aksiyonu engellemiyor (kod-bazlı koruma bilerek geri getirilmedi, [[co-ulaksec-guard-yes-ho-no]] kararıyla tutarlı).
- [ ] **web panel: satır durumu sadece "proc canlı mı" değil, CLI gerçekten iş yapıyor mu (busy/idle) göstermeli** (2026-08-28, kullanıcı: "eger cli de is varsa calisior gorunsun yoksa calsmiyor gibi") — durum hücresi (React'e geçişten sonra `SessionRow.tsx`, eski `py/claudeops/commands/web.py:1634` referansı artık geçersiz — o eski PAGE_HTML paneli 2026-09-01'de emekli edildi) şu an sadece `<span class="dot on">` + pid basıyor; session idle prompt'ta beklerken de aktif hesaplama yaparken de aynı görünüyor. `cpu%` kolonu (`s.cpu`) güvenilir bir busy sinyali değil (API yanıtı beklenirken network-bound, CPU düşük kalabilir).
  ⚠ **2026-09-04: canlı araştırıldı, KASITLI OLARAK UYGULANMADI — orijinal fikir (sabit "Computing…"/"esc to interrupt" string'i arama) CANLI TEST'TE YANLIŞ ÇIKTI, blind uygulansaydı sessizce hep-yanlış bir gösterge olurdu.** Throwaway bir tmux-only test session'ında (`busytest0904`, gerçek fleet'e dokunulmadan, iş bitince `tmux kill-session`) `claude` v2.1.260'a bilerek ~10-20s süren bir görev verilip `tmux capture-pane` ile ANLIK yakalandı. Bulgular:
    1. **Busy satırı sabit "Computing…" DEĞİL** — CLI her seferinde rastgele/rotating bir jerundiyum kullanıyor (canlı yakalanan örnek: `✢ Levitating…`, muhtemelen "Pondering/Simmering/..." gibi bir havuzdan). Sabit kelime eşleşmesi CLI güncellemesiyle her an sessizce kırılır (bu proje boyunca CLI zaten 2.1.169→2.1.241→2.1.260 gibi sık değişti) — [[TOBEDECIDED#10]]'daki "CLI'lar sabit durmuyor" dersiyle aynı aile.
    2. **Yanıltıcı ikiz (asıl tuzak):** bir tur biterken CLI AYNI görsel aileden (spinner-benzeri glyph + tek satır) bir "bitti" özeti de basıyor — canlı yakalanan: `✻ Worked for 23s · done 8:23`. Sadece "başında spinner glyph'i olan satır var mı" diye bakan bir regex bunu YANLIŞLIKLA busy sayardı — tam da özelliğin önlemeye çalıştığı hatayı (yanlış durum göstermek) tersinden yeniden üretirdi. Ayırt edici olan muhtemelen satır sonundaki `…` (ellipsis) — busy satırları hep `…` ile bitiyor, "Worked for Ns · done H:MM" satırı bitmiyor — ama bu tek örnekle doğrulanmış bir kural, kapsamlı değil.
    3. **Satırın ekrandaki konumu SABİT DEĞİL** — bir bash tool-call'ının çok satırlı çıktısı (`⎿ tick / tick / … +5 lines`) durum satırını `tmux capture-pane`'in küçük bir "son N satır" penceresinin dışına itebiliyor (canlı gözlemlendi: aynı görev sürerken art arda alınan `-S -6` capture'larda durum satırı bazen hiç görünmedi). Güvenilir olması için ya çok daha büyük bir capture penceresi (`-S -200` gibi) ya da farklı bir yaklaşım gerekiyor — ki bu da her poll'da (WS broadcaster + `/api/status`, TÜM çalışan session'lar için, sık aralıklarla) ekstra bir `tmux capture-pane` subprocess'i çalıştırmak anlamına gelir; şu anki `cpu`'nun ucuz `ps`-tabanlı ölçümüne kıyasla performans/maliyet etkisi ölçülmedi.
    **Sonuç:** fikir hâlâ geçerli/istenen ama fix live-verification OLMADAN yazılmamalı (bu dosyanın genel ilkesiyle aynı) — bir sonraki denemede yukarıdaki 3 bulgudan başlanmalı: yapısal bir regex (`^\s*\S\s+[A-Z][a-zA-Z]*…` gibi, "Worked for"/"done" içermeyen), daha geniş capture penceresi, ve per-poll maliyetini sınırlayan bir strateji (ör. busy-check'i ana status pollundan daha seyrek/ayrı bir döngüde yap, ya da sadece "kullanıcı o session'ın terminal modalını açtığında" hesapla).
- [ ] **web panel Terminal: modal açılışta oto-resize/font-fit "kayma" yaratıyor — sabit oran ya da oto-resize'ı kapatma seçeneği** (2026-08-28, kullanıcı: "cli web ui den acilinca resize etmeme izin olsun. belki kaymayi engelleyecek ratio buluruz"; 3 ekran görüntüsü `~/Pictures/Screenshots/Screenshot from 2026-08-28 08-40-4{9,53,58}.png`) — görüntülerde terminal içeriği satır satır kaymış/parçalanmış, aralarda büyük boşluklar oluşmuş görünüyor (okunaksız). `pollTerm` gerçek pane boyutu değiştiğinde (`d.cols/d.rows`; bkz. `tmux_pane_size` docstring'i, `py/claudeops/tmux_backend.py:124-129`: attach eden gnome-terminal istemcisi pane'i tmux'un `-x/-y` isteğinden BAĞIMSIZ kendi boyutuna zorluyor) `term.resize()` + `computeFitFontSize`/`fitContainerToTerm` (`web.py:1991-2025`) ile font-size'ı yeniden hesaplıyor; ama `tmux capture-pane` metni ZATEN kaynak pane'in o anki genişliğine göre hard-wrap edilmiş geliyor (`tmux_capture`, `tmux_backend.py:82`). xterm.js'in cols'u resize tetiklenene kadar bir tick GERİDE kalırsa (varsayılan 160'tan gerçek genişliğe geçiş anı gibi), eski genişlikte hard-wrap edilmiş metin yeni/yanlış cols'a yazılır → tam bu "shredded" görünüm. Kullanıcı isteği: oto-resize'ı kapatıp sabit bir cols/rows-to-px oranında tutma seçeneği (drift'i kaynağında önler). Fix adayı: (a) resize algılanan tick'te önce resize'ı uygula, capture'ı BİR SONRAKİ tick'e kadar yazma (yeni genişlikte capture gelsin); (b) kullanıcıya "oto-fit kapalı, sabit font-size" seçeneği/toggle ekle. **Güncelleme (2026-09-02, kod incelemesi):** (a) parçası react-ui'de zaten böyle — `TerminalView.tsx:206-211`'de `resized` tespit edilince `term.resize()` ÖNCE, `inst.term.write(result.text)` (o tick'in ZATEN doğru cols'a göre gelen capture'ı) SONRA çalışıyor, eski genişlikte hard-wrap edilmiş metnin yeni cols'a yazıldığı sıralama açığı yapısal olarak yok — "shredded" görünümün bizzat kendisi muhtemelen zaten düzelmiş (canlı/tarayıcıda doğrulanmadı, sadece kod-akışı doğrulandı). (b) — kullanıcının asıl istediği "oto-resize'ı kapat" toggle'ı — HÂLÂ UYGULANMADI; bug'ın kendisi düzeldiyse bu toggle'a ihtiyaç kalmamış olabilir, canlı doğrulanana kadar açık bırakıldı.
- [ ] **web panel Terminal: içteki scrollbar'ı fare ile sürükleyip yukarı/aşağı çekemiyorum** (2026-08-28, kullanıcı: "icteki scrolla basip asagiya yukariya cekrmiyorum"; kullanıcı: acil değil) — muhtemelen xterm.js'in kendi scrollbar'ı/`.xterm-viewport` ile ilgili bir CSS/etkileşim sorunu; araştırılmadı, düşük öncelik.
- [x] **web panel "Kayıtlı" (Registered) sekmesi: gruplama/tree yok, aynı gruptan çalışan var mı görünmüyor ✅ DONE** (2026-09-02) — (2026-08-28, kullanıcı: "Registered da bu grupda acik olan var mi gosterelim. Ayrica registered tree seklinde olsun. Hangi grupda neler var gorelim.") Netleştirme sorusu kullanıcıya soruldu, cevap: **cwd bazlı** (yeni roster kolonu değil). Fix: `RegisteredTab.tsx`'e `groupByCwd()` — satırlar cwd'ye göre gruplanıp her grup bir katlanabilir başlık satırı (▸/▾, varsayılan AÇIK) + o cwd'yi paylaşan İSTER çalışan İSTER durmuş HERHANGİ bir session varsa (farklı bir isimle bile olsa, ör. bu repodaki cops+diag) amber "bu grupta çalışan var" rozeti alıyor. Sayfalama (aşağıdaki L74) bu sekmede TEK TEK satır değil GRUP bazında uygulanıyor — bir grubun isimleri asla iki sayfaya bölünmüyor.
- [x] **autostart cold-boot hâlâ bash `claudeops` ✅ DONE** (2026-09-02) — (2026-06-25, GÜNCELLEME 2026-08-30: fix yönü değişti) `~/.config/autostart/claudeops.desktop` reboot'ta `bash -lc 'claudeops guard --boot --lock'` (BASH tool) çalıştırıyordu. Eski plan "py/cops guard --boot --lock'a taşı" idi; kullanıcı (2026-08-30) haklı olarak itiraz etti: artık gerek yok — `py/cops service` ile web panel+tunnel zaten reboot'ta kendiliğinden ayakta, fleet'i telefondan panelden tek tek başlatıyor. **Uygulanan fix: `.desktop` dosyası kaldırıldı** (silmeden önce kontrol edildi: içinde zaten `X-GNOME-Autostart-enabled=false` vardı — yani fiilen inert'ti, silme sadece cruft temizliği, davranış değişikliği yok). Bkz. [[TOBEDECIDED#12]] (bash'in geri kalanının kaderi hâlâ ayrı/açık soru). [[reboot-recovery]] [[guard-cron-relative-path]]
- [ ] **`deep-ho` komutu (yeni, ayrı cmd)** — 2026-06-01 istek. Normal `ho` sadece wrap-up (commit/push + MD güncel mi) sorar. `deep-ho` ek olarak: her CLI/session'ın TÜM jsonl geçmişini okuyup "kaçırdığımız bir şey var mı?" analizini yaptırır (yarım kalan iş, kaydedilmemiş karar, eksik test/doküman, TODO'ya yazılmamış fikir). Tek komut hem `ho` hem `deep-ho` fazını çalıştırabilmeli (`ho --deep` veya `deep-ho` ayrı dispatch). Her session'a daha uzun/derin bir wrap-up prompt'u gider; çıktı per-session özet + co'ya toplanır.
- [x] **`boot`/`recover` `models.tsv` lookup — py tarafı DONE, bash açık kaldı** (2026-06-01 → 2026-09-03 py kısmı kapatıldı) — Python'un boot/crash-recovery karşılığı ayrı bir `boot` komutu değil, `guard.py`'nin `guard_once()`'ı (bkz. bugünkü [[default_model_for]] fix'i): `model = models.get(entry.name) or entry.model or default_model_for(...)` — models.tsv KESİNLİKLE önce kontrol ediliyor, tam istenen davranış. Bash'in `cmd_boot`/`BOOT_MODEL_DEFAULT`'ı hâlâ ayrı/açık (TBD#12'nin bash-kaderi sorusuna bağlı, tek başına düzeltmeye değmez).
- [x] **`--model` verince default `--permission-mode=auto` — py'de zaten böyle** (2026-05-25 → 2026-09-03 kapatıldı) — kontrol edildi: `rc.py`'de `permission_mode = args.permission_mode or provider.permission_modes()[0]`, `handover.py`/`web.py`'nin TÜM spawn call-site'larında aynı desen — `--permission-mode` verilmezse (model verilsin verilmesin) zaten `provider.permission_modes()[0]` = `"auto"`'ya düşüyor. Bash-döneminden kalma, py tarafında baştan beri çözülmüştü.
- [x] **Python UI (büyük) — DONE (React web panel)** (2026-09-03 kapatıldı) — asıl istenen ("session listesini göster, tıkla → compact/RC/kill/send butonları") `py/cops web` + React panel ile fazlasıyla karşılandı (bkz. TOBEDECIDED#14 "react-ui ASIL"). "layout görsel önizleme" (pencere yerleşiminin görsel ön-izlemesi) TEK eksik kalan alt-parça — ayrı, küçük bir TODO olarak değerlendirilebilir ama ana madde kapandı.
- [ ] **`claudeops history` + `claudeops launch <name|sid>`** — geçmişte açık olan TÜM session'ları registry'le, `launch` ile yeni gnome-terminal'de RC açar. `launch <name>` kısmı zaten panelin "Başlat"/reactivate akışıyla karşılanıyor; asıl eksik **`<sid>`ile belirli bir tarihi/geçmiş konuşmayı** (en-yeni değil) hedefleyip açabilmek — bugün `resolve_resume_id`/`find_latest_jsonl` hep EN YENİ jsonl'i seçiyor, belirli bir sid'i seçme yolu yok. Küçük değil, tasarım netleşmeden (hangi sid'ler "geçmiş" sayılır, UI'da nasıl listelenir) uygulanmamalı.
- [x] **`--models=name:model,...` per-name config — DONE (models.tsv)** (2026-09-03 kapatıldı) — bu tam olarak `~/.claude/claudeops/models.tsv`'nin (name→model TSV, `read_models()`) yaptığı şey; ayrı bir `--models=` flag'ine gerek yok, TÜM py komutları (rc/guard/handover/web) zaten bu dosyayı okuyor.
- [ ] **Spawn/layout geometry: KİLİTLİ ekran → Mutter pencere-move BOZUK (KANITLANDI 2026-06-23/24)** — ~~hipotez~~ artık kesin: kilitli ekranda `layout` pencereleri sola üst üste yığıyor, `xdotool windowmove` TAMAMEN reddediliyor, `wmctrl -e` 2×-offset veriyor (sahte "200% scale"), root-screenshot blank. 2026-05-17 HDMI-rows da aynı sebep. **SAATLERCE kaybettirdi** — koordinat/scale "fix"i yanlış yola sokar (artefakt). **Fix:** `py/cops layout` (ve spawn-geometry) başına pre-flight lock-check: `loginctl show-session $(loginctl --no-legend|awk '{print $1;exit}') -p LockedHint` → `yes` ise abort + "önce ekranı unlock et" uyar. [[layout-needs-unlocked-screen]]
- [x] **`layout`: çoklu-monitor kurulumunda pencereleri YANLIŞ ekrana/köşeye yığıyor ✅ DONE (2026-09-03, b2dcfd7)** — detay DONE.md'de taşındı ("layout çoklu-monitor pile-up fix — canlı doğrulandı").
- [ ] **`layout` ayarlarına "hedef ekran" seçeneği (ana/ek/ikisi) + genel ayar-yüzeyi genişletme** (2026-08-28, kullanıcı: "layout derken pencereleri hangi ekrana yigmali 1-asil ekran, 2-ek ekranlar 3-ikisini birden kullan gibi ayarlarimiz olmali + baska ne gibi ayarlar olabilir dusun ve onlari da ayarlar tabina ekle") — yukarıdaki çoklu-monitor bug'ının düzgün çözümü zaten bunu gerektiriyor: `_run_layout`'ta (`py/claudeops/commands/web.py:680`) hâlâ kullanılmayan bir `screen_y` parametresi var ama web panelinin Layout sekmesi (`renderLayoutBox`, `web.py:2245-2263`) bunu HİÇ göstermiyor/göndermiyor (`doLayout()`, `web.py:2266` POST body'sinde yok) — yani backend'de yarım bırakılmış bir kanca zaten mevcut. Önerilen: monitör enumerasyonu netleşince (yukarıdaki bug'a bkz.) Layout sekmesine bir seçici ekle: **ana ekran / ek ekran(lar) / ikisi birden**. Kullanıcının istediği "başka ne olabilir" beyin fırtınası (henüz uygulanmadı, sadece öneri — netleşince seçilip implementasyona geçirilebilir):
  - **Izgara yoğunluğu** (per-desktop pencere sayısı — bash'teki `per` değişkeninin karşılığı; şu an web'de sabit/gizli) — 2×2 dışında 3×2/3×3 vb. seçilebilir olsun.
  - **"BR köşeyi hep boş bırak"** kuralı — zaten ayrı açık bir TODO ("Geliştirme" bölümünde), aynı ayar-yüzeyine eklenebilir.
  - **Başlangıç workspace/desktop no'su** — zaten ayrı açık bir TODO (`layout --group` desktop no'su serbest-other sayısına bağlı), sabit hedefleme seçeneği aynı forma girebilir.
  - **Ekran-kilit ön-kontrolü göstergesi** ([[layout-needs-unlocked-screen]]) — apply'dan önce "ekran kilitli/açık" durumunu panelde canlı gösterip kilitliyken butonu disable etmek (şu an sadece backend'de sessiz `_screen_locked()` reddi var — kullanıcı reddin NEDENİni panelde görüyor mu kontrol edilmeli).
  - **Monitör başına farklı ızgara yoğunluğu** (büyük ek ekranda 3×2, laptop'ta 2×2 gibi) — daha büyük/isteğe bağlı bir adım, ilk sürümde şart değil.
- [x] **Kullanıcı-bazlı kalıcı ayarlar (Settings): ho için varsayılan effort/model, yeni/resume için varsayılan model, app teması, vb. ✅ DONE** (2026-09-02) — (2026-09-01, kullanıcı: "these kind of settings must be kept for the user. default model for ho, default model for new resume etc. app theme etc"). Tasarım sorusu kullanıcıya soruldu, cevap: **sunucu-taraflı** (`~/.claude/claudeops/settings.json` — roster.tsv/models.tsv'yle aynı desen, TÜM cihaz/tarayıcılardan aynı görünür). Fix: yeni `py/claudeops/settings.py` (`load_settings`/`save_settings`, atomik yazma, bilinmeyen anahtarları sessizce atar) + `/api/settings` (GET yok, sadece POST — `_status_payload()`'ın `settings` alanı zaten her poll'da taze okuyor) + yeni "Ayarlar" sekmesi (`SettingsTab.tsx`). Kapsanan: (a) handover varsayılan effort'u — `default_handover_effort()` artık `load_settings()`'e bakıyor (override provider'ın `effort_levels()`'ında VARSA kullanılır, yoksa eski 'high'-tercih mantığı; Faz1+Faz2+web handover+yeni compact'ın (aşağıya bkz.) HEPSİ bu TEK fonksiyonu kullandığı için sıfır call-site değişikliğiyle otomatik kapsandı); (b) handover'ın "varsayılan modeli" ayrı bir ayar OLARAK EKLENMEDİ — `_handover()` zaten session'ın MEVCUT modelini koruyor (yeniden seçmiyor), o yüzden bu alt-madde kapsam dışı bırakıldı (orijinal istek muhtemelen (c)'yle karışmıştı); (c) yeni-chat/resume için CLI-başına varsayılan model — `OptionsRow.tsx`'in `model` state'i artık `settings.default_model[cli]`'den ön-doluyor (hâlâ her seferinde değiştirilebilir, sadece başlangıç değeri); (d) app teması (system/light/dark) — `data-theme` attribute + `global.css`'e açık override blokları + `localStorage` fast-path (ilk `/api/status` yanıtından önce yanlış tema flash'ını önlemek için, `cops_lang` ile aynı desen) + diğer açık tab/cihazlara WS push ile anında yansıma. **Güncelleme (2026-09-03):** (b)'nin gerekçesi doğruydu (mevcut session'ın modelini korumak) ama kapsam-dışı bırakılan asıl şey farklı bir gedikti — `default_model[cli]` DEĞERİ o zamandan beri backend'de HİÇ okunmuyordu, sadece `OptionsRow.tsx`'in ön-dolu değeriydi (yani ör. guard/stuck-recovery/Faz1/Faz2/register/new-chat gibi model BİLİNMEDİĞİ TÜM yollar Ayarlar'ı atlayıp doğrudan `provider.model_choices()[0]`'a, guard.py/stuck.py ise hatta ondan da geçip stale sabit `"claude-sonnet-4-6"`'ya düşüyordu). Kullanıcı isteğiyle ("her cli providerin default varsayılan modeli settings de olmalı") kapatıldı: bkz. DONE.md.
- [x] **Tablara sayfalama (pagination) ✅ DONE** (2026-09-02) — (2026-09-01, kullanıcı). Kapsam sorusu kullanıcıya soruldu, cevap: **4 liste sekmesinin hepsi (Çalışanlar/Kayıtlı/Devre Dışı/Emekli), 20/sayfa**. Fix: paylaşımlı `hooks/usePagination.ts` (saf/türetilmiş — `page` DEĞİL sadece state, `totalPages`/`pageItems` her render'da hesaplanır; liste küçülünce sayfa numarası kendiliğinden geri çekilir) + paylaşımlı `components/shared/Pagination.tsx` (1 sayfaya sığınca hiçbir şey render etmez). `RunningTab`/`GroupTable` (Devre Dışı+Emekli ikisi de bunu paylaşıyor) satır bazında, `RegisteredTab` yukarıdaki gruplama kararıyla birlikte GRUP bazında sayfalanıyor. ⚠ Canlı doğrulama sırasında (`npm run lint`) gerçek bir bug yakalandı+düzeltildi: `usePagination()` hook'u ilk halinde `if (!data) return null` guard'ından SONRA çağrılıyordu — React'in "hooks unconditionally" kuralını ihlal ediyordu (data null→dolu geçişinde runtime hatası verirdi); hook çağrısı guard'dan ÖNCEYE taşındı (`data ? ... : []` fallback'iyle).
- [x] **Tabların üstüne arama kutusu ✅ DONE** (2026-09-04, kullanıcı: "tabların üstüne arama kutusu") — yeni `components/shared/SearchBox.tsx`, `App.tsx`'te `<Banners>` ile `<TabBar>` arasına (yani gerçekten tabların ÜSTÜNE, herhangi bir sekmenin içine değil) yerleştirildi — tek bir `search` state'i (`AppShell`) tab değiştirince SIFIRLANMIYOR, 4 liste sekmesinin (Çalışanlar/Kayıtlı/Devre Dışı/Emekli) hepsini VE `TabBar`'ın kendi sayaçlarını aynı anda süzüyor. Süzme mantığı paylaşımlı `components/shared/searchFilter.ts`'in `matchesSearch()`'ü — isim VEYA cwd'de case-insensitive substring (boş sorgu = herkes eşleşir, yani mevcut davranış search kullanılmadıkça birebir korunuyor). `RunningTab`/`RegisteredTab` kendi running/!running filtresinden SONRA süzüyor (`RegisteredTab` süzmeyi `groupByCwd`'den ÖNCE yapıyor, böylece hiç üyesi kalmayan bir grup hiç render edilmiyor); `GroupTable` (Devre Dışı+Emekli paylaşıyor) içeride kendi süzüyor. Select-all/BulkBar/Pagination'a AYRICA dokunulmadı — hepsi zaten "`rows`/`items` her neyse onun tamamı" varsayımıyla yazılmıştı (pagination'ın kendisi de aynı desen), search sadece bu değişkenin tanımını daralttı. Boş-durum mesajları ayırt edici: liste GERÇEKTEN boşsa eski mesaj (`nothingRunning`/`noneRegistered`/`empty`), doluyken sadece arama hiçbir şeyle eşleşmiyorsa yeni `noSearchMatches` (TR/EN). **Doğrulama:** `npm run typecheck` (`tsc -b`, temiz) + `npm run lint` (oxlint — bu vesileyle küçük bir yan bug da bulunup düzeltildi: `.oxlintrc.json` var ama hiçbir ignore kısıtı yoktu, script'in kendisi (`oxlint`, path'siz) git-tracked `dist/`'teki minified bundle'ı da tarıyor, yüzlerce alakasız uyarı basıyordu — yeni `py/webui/.eslintignore` (`dist`) ekleyip `npm run lint`'in KENDİSİNİ düzelttim, artık sadece `src/`'i tarıyor). Değişen/yeni dosyalarda (`searchFilter.ts`, `SearchBox.tsx`, `App.tsx`, `TabBar.tsx`, `RunningTab.tsx`, `RegisteredTab.tsx`, `GroupTable.tsx`) SIFIR yeni uyarı — mevcut 6 uyarı hepsi alakasız/önceden-var-olan dosyalarda). `npm run build` BİLEREK çalıştırılmadı ([[webui-build-is-live-deploy]] — `dist/` git-tracked + canlı servis oradan serve ediyor, commit-onayı öncesi build = istemeden canlı deploy).
- [x] **Provider feature-parity audit: her provider'ın her özelliği implemente edilmiş mi? ✅ AUDIT TAMAMLANDI (1 gerçek gap tespit edildi + 1 mimari ihlal düzeltildi)** (2026-09-04, kullanıcı: "todo: her provider ın her ozelliği implemente edilmiş mi?") — `providers/base.py`'nin `CliProvider` arayüzü (8 zorunlu abstract method + 4 opsiyonel override: `has_conversation`/`last_exchange`/`full_history`/`env_overrides`) 4 provider'a (claude/agy/codex/shell) karşı tek tek karşılaştırıldı.
  - ✅ 8 abstract method'un HEPSİNİ 4 provider da tam implemente ediyor (Python'un ABC mekanizması zaten garanti eder, grep ile teyit edildi).
  - ✅ `has_conversation()` doğru ayrıştırılmış (sadece `shell` False — gerçek konuşma kavramı yok), `env_overrides()` ihtiyacı olan 3 provider'da (agy/codex/shell) var, claude'un ihtiyacı yok — gap yok.
  - ⚠️ **GERÇEK GAP (uygulanmadı, sadece tespit+belgelendi): `last_exchange`/`full_history` (panelin "Sohbet" sekmesi) SADECE `claude` implemente ediyor**, agy/codex/shell hepsi `None` (desteklenmiyor) dönüyor. Araştırıldı: agy'nin HER konuşması `~/.gemini/antigravity-cli/conversations/<uuid>.db` altında AYRI bir SQLite dosyası (`cache/last_conversations.json`'daki cwd→id eşlemesiyle bulunuyor) — şema incelenmedi (auto-mode classifier canlı bir conversation DB'sini açmayı reddetti, kişisel konuşma içeriği olduğu için haklı bir temkin). Codex'inki DAHA kolay görünüyor: `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`, claude'un kendi formatına çok benzeyen satır-bazlı JSON (`codex_provider.py`'nin `_session_meta()`'sı zaten ilk satırı `type`'a göre okuyor) — muhtemelen en az efor gerektiren taraf. Değeri var ama gerçek şema-okuma + iki ayrı parser + canlı doğrulama gerektiriyor — bu bir "audit", implementasyon değildi.
  - ✅ **FIX (mimari ihlal, düzeltildi):** `web.py`'nin `_compact()`'ı `if chosen_cli != "claude": return _err(...)` ile HARDCODED bir gate kullanıyordu — `CliProvider`'ın kendi kuralını ("manager kodunda `if cli==X` dallanması YOK") ihlal ediyordu, `last_exchange`/`full_history`'nin "None=desteklenmiyor, sadece destekleyen override eder" desenine UYMUYORDU. Yeni `CliProvider.compact_command() -> Optional[str]` (varsayılan `None`, `ClaudeProvider` `"/compact"` döndürür) eklendi — `_compact()`'in hem gate'i hem headless çağrısının kendisi (`-p provider.compact_command()`) artık bu metoda dayanıyor. Davranış DEĞİŞMEDİ (compact hâlâ SADECE claude'da çalışıyor, hardcoded string yerine polimorfik gate) — pür mimari-hizalama, sıfır gözlemlenebilir fark. Headless çağrının argv/binary şekli BİLEREK genellenmedi (`shutil.which("claude")` hâlâ sabit) — bugün var olmayan bir ikinci veri noktasına göre spekülatif genelleme yapılmadı, gerçekten başka bir provider compact kazanırsa o zaman.
  - Yan bulgu: `api/types.ts`'teki `TermChatResult` yorumu "agy/shell today" diyordu, codex'i unutmuştu (o da desteklemiyor) — düzeltildi.
  - Doğrulama: `python3 -m py_compile` (3 değişen .py dosyası, temiz).
- [ ] **Auto-respond permission prompts** — OCR çalışıyor (tesseract), keystroke landing intermittent. Seçenekler: OCR + RC API inject, ydotool (Wayland), claude TUI `--auto-accept` flag.
- [ ] Wayland desteği için layout fallback (gdbus + Mutter extension veya hint)
- [ ] Terminal emülatör parametrize (gnome-terminal yerine kitty/alacritty config/env)
- [ ] Rate-limit reset zamanını output'tan parse + auto-resume
- [ ] `claudeops batch --dry-run`
- [ ] `claudeops list --json` machine-readable
- [ ] `claudeops send` stdin'den prompt okuma
- [ ] `claudeops layout` için "BR köşede her zaman boş bırak" benzeri kural
- [ ] **handover `--layout` oto-tile `--group` geçirmiyor** — tek-komut `handover --force --layout` yolunda grup'lar uygulanmaz (ayrı Faz 3 komutu --group içeriyor). Fix: `cmd_handover`'a `--group=` passthrough → `cmd_layout`'a ilet. (2026-05-31, --group eklenince fark edildi.)
- [ ] **`layout --group` desktop no'su serbest-other sayısına bağlı** — 16 serbest-other → grup ws4/5; sayı değişirse kayar (gruplar yine birlikte ama ws no farklı). Sabit istenirse `--group=names@ws` hedefleme ekle (others o ws'i atlar). (2026-05-31)
- [x] UI'ye Handover textini o an hangi dil secili ise o dilde gosterm tabi. ordan copy paste yapariz. Gerektiginde ayri cli acmadan. **✅ DONE (2026-09-02)** — `_status_payload()`'a `handover_msg: {tr, en}` eklendi (`_handover()`'ın zaten gönderdiği aynı iki sabit); Tanı sekmesine (`DiagnosticsTab.tsx`) seçili dile göre metni gösteren kaydırılabilir bir kutu + kopyala butonu eklendi.

## Dokümantasyon

- [ ] README'ye actual workflow örnekleri (güncel session isimleri) — **kısmen ilerledi (2026-09-02):** kullanıcının ntfy app ekran görüntüleri üzerinden `service notify`'ın gerçek bildirim formatı (`claudeops tunnel: https://...`) + güvenilirlik kısıtları (Android arka-plan/ntfy.sh saklama süresi) 4 README'ye de (kök + `py/`, TR+EN) eklendi. Kalan: session isimleriyle diğer workflow'ların (handover, compact, layout vb.) gerçek örnekleri hâlâ yok.
- [ ] CLAUDE.md'ye "ne zaman compact" rehberi
- [ ] Demo gif/video — visible mode reopen sırası

## Test/Quality

- [ ] Unit test: `find_self_claude_pid` (claude değil bash'tan çağrılınca)
- [ ] Smoke test: tek session aç, kill, compact, RC, doğrula
- [ ] Edge case: 0 session açıkken komut davranışı

## Açık sorular

- [x] **gnome-terminal `--title` flag'i claude TUI tarafından override mı? ✅ CEVAPLANDI (2026-09-04)** — evet. `claude --help` (v2.1.260): `-n, --name <name>` → "Set a display name for this session (shown in the prompt box, /resume picker, **and terminal title**)". Yani claude spawn olduktan sonra kendi terminal title'ını KENDİ `-n`/session-adından set ediyor; gnome-terminal'in spawn-anındaki `--title` bayrağı sadece claude başlayana kadarki ilk anlık görünüm, claude ilk title-escape'ini yazar yazmaz EZİLİYOR. Bu zaten [[stale-tui-title-cross-suffix-resume]]'un temel mekanizmasını açıklıyor (title `-n`'den BİR KERE set ediliyor, resume'da güncellenmiyor → eski suffix'te donuk kalıyor).
- [ ] `--remote-control` flag'i `--name` ile çakışıyor mu? (RC name session name'i de set ediyor görünüyor) — **kısmen aydınlandı (2026-09-04):** `claude --help`'te ikisi AYRI flag — `-n, --name <name>` ve `--remote-control [name]` ("optionally named") birbirinden bağımsız, ikisi de kendi opsiyonel isim argümanını alıyor. Projenin kendi convention'ı (CLAUDE.md: `-n NAME --remote-control NAME 'PROMPT'`) ikisine hep AYNI değeri veriyor — ki bu, ikisinin gerçekten bağımsız/senkronize-tutulması-gereken alanlar olduğu varsayımını destekler. AMA iki flag'e FARKLI değer verilirse ne olacağı (hangisi kazanır, gerçekten çakışıyor mu) canlı test EDİLMEDİ — bunun için gerçek bir interaktif RC-enabled spawn + sonucu inceleme gerekir (headless `-p` bu yolu muhtemelen hiç tetiklemiyor, "interactive session" gerektiriyor), bu turda yapılmadı. Açık kalıyor.
